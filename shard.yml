name: cogni-ml
version: 0.40.0

authors:
  - Sergey Kuznetsov <oss@rigelstar.com>

description: |
  Crystal ML library with native Metal GPU acceleration.
  GGUF model inference, BERT embeddings (43ms on M2 Max),
  autograd, NN layers, optimizers, llama.cpp bindings.

crystal: ">= 1.10.0"

license: MIT

targets:
  spec:
    main: spec/spec_helper.cr
