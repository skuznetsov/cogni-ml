Write Crystal code implementing module Answer with self.stable_unique(values : Array(Int32)) : Array(Int32).
Return each distinct integer once, preserving first occurrence order. Do not mutate the input. Handle empty input, negative integers and repeated values. Use only the standard library; no I/O, files, processes or macros. Output only one complete Crystal code block, no explanation, no tests. Keep the implementation short. This is a pure library function, not a command-line program. The function must work for arbitrary arrays, not just an example. Include any standard-library require needed for your chosen container.
