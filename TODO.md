# TODO — Qwen 3.5/3.6 Native Metal Port

**Goal:** Native Metal inference for Qwen 3.5 9B and 3.6 27B (arch=qwen35), targeting ≥10% faster than llama.cpp HEAD on decode, with cosine≈1.0 correctness.

**Target model:** `Qwen3.5-9B-Q4_K_M.gguf` (primary debug target) → scale to `Qwen3.6-27B-Q4_K_M.gguf`.

**llama.cpp baseline source:** `~/SrcArchives/AI/llama.cpp` (HEAD).

**Protocol:** v13.2. Quadrumvirate on each phase start. LOGBOOK in conversation, persistent state in LANDMARKS.md.

---

## Phase 0 — Verification & infrastructure

- [x] **0.1** Create LANDMARKS.md + TODO.md
- [x] **0.2** Survey llama.cpp HEAD for: qwen35 arch implementation, DeltaNet (not Mamba), M-RoPE, Q4_K Metal kernels
- [x] **0.3** llama.cpp HEAD baseline: 9B Q4_K_M pp64=458, tg64=43.5 tok/s on M2 Max (FA=0)
- [x] **0.4** qwen35 hparams parsing: `get_int_array` helper + `Qwen35Hparams` struct; 5 spec examples passing

## Phase 1a — Q4_K CPU path

- [x] **1a.1** `Dequant.dequantize_q4_k` in `src/ml/gguf/dequant.cr` (block: d/dmin/scales[12]/qs[128] = 144 B)
- [x] **1a.2** `QuantMatmul.matmul_add_q4k` in `src/ml/gguf/quant_matmul.cr`
- [x] **1a.3** Correctness vs libggml: dequant bit-identical (cos=1.0, max_diff=0) on ssm_alpha/ffn_up; fused matmul cos≈1.0, max_diff≈2.4e-7 on 4096×4096

## Phase 1b — CPU-reference Qwen 3.5 9B forward

- [ ] **1b.1** `CausalDecoder` extensions for qwen35: partial M-RoPE with sections, GQA 4:1 broadcast, Mamba SSM layer, full-attn every 4 layers, SwiGLU FFN, RMSNorm
- [ ] **1b.2** LM head with Q4_K
- [ ] **1b.3** Correctness: cosine≈1.0 vs llama.cpp across all 32 layers on 1 token

## Phase 2 — Metal Q4_K kernels

- [ ] **2.1** `dequantize_q4_K_fn` in Metal (Q5_K pattern minus qh)
- [ ] **2.2** `simd_gemm_q4k` + `simd_gemm_q4k_moe` in `gemm_simd.metal`
- [ ] **2.3** `simd_mm_q4k` + `simd_mm_q4k_moe` + `batched_mm_q4k` in `gemm_mm.metal`
- [ ] **2.4** Correctness: cosine≈1.0 vs CPU Q4_K, speedup vs reference

## Phase 3a — Metal attention: head_dim=256 + GQA

- [ ] **3a.1** Read llama.cpp M-RoPE implementation — document exact formula (**BLOCKER for 3a**)
- [ ] **3a.2** Rewrite `attention_causal.metal`: dynamic head_dim (no fixed `float4 qr[16]`), kv-head broadcast
- [ ] **3a.3** Rewrite `attention_flash.metal`: same + flash version
- [ ] **3a.4** Partial M-RoPE fused in QKV
- [ ] **3a.5** Correctness: full-attn layer 9B cosine≈1.0, no regression on nomic BERT

## Phase 3b — Metal Mamba

- [ ] **3b.1** SoTA harvest: Mamba-2 paper, llama.cpp ggml-cuda/mamba.cu, reference selective_scan
- [ ] **3b.2** `selective_scan_fwd` Metal kernel
- [ ] **3b.3** SSM state management in BufferPool
- [ ] **3b.4** Correctness: SSM layer 9B cosine≈1.0

## Phase 3.5 — State save/load

- [x] **3.5.1** Serialize: KV-buffers (full-attn layers) + DeltaNet recurrent state (conv + SSM) + metadata
- [x] **3.5.2** Deserialize / restore
- [x] **3.5.3** Prompt KV cache: exact lookup by session/hash, artifact restore, fail-closed artifact hash/model checks
- [x] **3.5.4** pg_sorted_heap metadata adapter for prompt KV cache (external artifact path + sorted session/hash index)
- [x] **3.5.5** Correctness: exact top1 + full-logit close restore on 9B
- [x] **3.5.6** Longest-prefix prompt cache: token-hash prefix lookup + exact suffix replay

## Phase 4 — Optimization: beat llama.cpp

- [ ] **4.1** Fused RMSNorm + partial M-RoPE + QKV projection
- [ ] **4.2** Fused SwiGLU
- [ ] **4.3** Weight-tile cache in threadgroup memory
- [ ] **4.4** ComputeGraph wave scheduling for decoder
- [ ] **4.5** Benchmark: ≥10% faster than llama.cpp HEAD on 9B decode
- [x] **4.6** Exact first-run prefill shortcut: skip output RMSNorm/lm-head for non-final prompt tokens
- [x] **4.7** Port llama.cpp-style chunked fused DeltaNet scan primitive
- [x] **4.8** Port chunked recurrent prep primitives: conv-shift, alpha/beta, Q/K L2 norm
- [ ] **4.9** True layerwise/microbatch prefill for known prompt tokens
  - [x] Experimental CPU-orchestrated layerwise recurrent chunk path (`QWEN35_PREFILL_CHUNK=1`) with correctness gate
  - [x] Falsifier: CPU-orchestrated chunking is slower than current whole-token Metal decode wave; keep default off
  - [x] GPU-resident recurrent layer chunk primitive; default-on prefill chunking improves pp64 p50 from `52.87 tok/s` to `56.70 tok/s`
  - [x] Reuse Q4_K simdgroup-matrix GEMM inside GPU-resident recurrent prefill chunks; pp64 p50 improves from `56.70 tok/s` to `70.38 tok/s`
  - [x] Chunk full-attention prefill layers on Metal; pp64 p50 improves from `70.38 tok/s` to `143.18 tok/s`
  - [x] Enable Q5_K/Q6_K batch GEMM in prefill chunks with GPU-side F32↔F16 conversion; pp64 p50 improves from `143.18 tok/s` to `278.50 tok/s`
  - [x] Keep DeltaNet chunk state rows register-resident across the token scan; pp64 p50 improves from `278.50 tok/s` to `308.86 tok/s`
  - [x] Keep consecutive recurrent prefill layers GPU-resident; pp64 p50 improves from `308.86 tok/s` to `327.54 tok/s`
  - [x] Batch the final prompt token into prefill and run only fused lm-head top1; pp64 p50 improves from `308.82 tok/s` to `358.44 tok/s`
  - [x] Fast-path full Q4_K GEMM output tiles with direct simdgroup stores; pp64 p50 improves from `358.44 tok/s` to `373.80 tok/s`
  - [x] Fuse full-attention chunk boundaries into following recurrent runs; total pp64 syncs drop from `17` to `10`, warm profile improves `177.20 ms` → `172.53 ms` (small/noisy but positive paired A/B)
  - [x] Add prefill attribution harness and matmul shape counters; pp64 shows FFN up/gate Q4_K dominates logical weight traffic (`1728 MiB`), Q6/Q4 FFN-down follows (`1062 MiB` combined), and Q5/Q6 batch GEMM is mandatory (`~170 ms` default vs `~367 ms` with `QWEN35_Q56K_BATCH_GEMM_OFF=1`)
  - [x] Add `--prepare-state` to the prefill attribution harness so kernel/profile work can measure prepared-state prompt ingest separately from lazy first-touch state allocation; pp64 prepared profile measured `142.31 ms` p50 / `449.72 tok/s` with the same hot-shape mix.
  - [x] In-place SwiGLU staging for chunked prefill FFN paths; exact aliasing path passes specs and small pp64 A/B improves default by `~0.48 ms` vs `QWEN35_SWIGLU_INPLACE_OFF=1`
  - [x] Falsifier: dual Q4_K gate/up+SwiGLU GEMM reduced dispatch count but was slower at pp64 (`174.78 ms` default vs `168.65 ms` off), likely from register pressure and extra barriers
  - [x] Specialize final full-attention prefill layer for last-row logits; final layer still writes all K/V but computes attention+FFN only for the final row, improving pp64 A/B from `173.86 ms` off to `170.00 ms` default
  - [x] Falsifier: direct SwiGLU-to-F16 activation staging for Q6 FFN-down was exact in focused specs but slower at pp64 (`170.02 ms` default vs `167.52 ms` off), so activation staging is not the current wall
  - [x] Falsifier: double-buffered Q6_K GEMM compiled and passed focused forward specs, but pp64 A/B was neutral (`168.22 ms` default vs `168.25 ms` off; p50 slightly worse), so Q6 barriers are not the next leverage point
  - [x] Raise default prefill chunk size from `64` to `1024`; pp256 A/B improves from `711.92 ms` p50 at chunk 64 to `522.76 ms` p50 default, while pp64 is unchanged within noise
  - [x] Batch the final long-prompt suffix instead of falling back to single-token final decode when prompt exceeds chunk size; pp2048 A/B improves from `4987.29 ms` p50 off to `4750.20 ms` default
  - [x] Raise default prefill chunk size again from `1024` to `2048`; pp2048 p50 improves from `4755.48 ms` at chunk 1024 to `4578.57 ms` at chunk 2048, while pp1024 is unchanged within noise
  - [x] Raise default prefill chunk size from `2048` to `4096` for long prompts; pp4096 one-shot p50 improves from `11862.78 ms` at chunk 2048 to `11524.63 ms` at chunk 4096
  - [x] Make default prefill chunk size memory-aware; 64GB M2 Max selects `8192` and pp8192 one-shot A/B improves from `35219.27 ms` at chunk 4096 to `33981.42 ms` default, while smaller systems fall back to `4096`/`2048`
  - [x] Add grouped command-buffer attribution for fused prefill waves; pp256 shows all `full+rec` groups are nearly flat (`~61.2-61.7 ms` wait) and `rec0-2` is smaller (`~46.4 ms`), so there is no single pathological layer group to attack
  - [x] Add scoped matmul attribution for prefill/decode phases; pp256 confirms `prefill.rec.ffn_upgate` dominates logical weight traffic (`1296 MiB`), followed by recurrent Q5 projection (`528 MiB`) and recurrent FFN-down (`796.5 MiB` combined Q6/Q4); decode prompt64/gen4 shows the same recurrent FFN/upgate and projection dominance (`5184 MiB` and `2112 MiB`)
  - [x] Harden prefill A/B attribution: `--compare-env` now runs paired interleaved trials and reports pair wins, avoiding block-order drift false positives
  - [x] Falsifier: lowering the GEMM route threshold so b8 prefill uses Q4/Q5/Q6 GEMM switches the routes correctly but slows pp8 (`140.70 ms` p50 default GEMV-at-b8 vs `158.74 ms` p50 GEMM-at-b8); keep `GEMM_BATCH_THRESHOLD=8`
  - [x] Falsifier: fusing final full-attn-last and lm-head top1 into one command buffer reduced pp64 syncs (`10` → `9`) but slowed wall reps in sequential release checks (`162.32 ms` p50 off vs `165.67 ms` p50 fused), so the removed sync is not worth the larger final command buffer
  - [x] Falsifier: a 256-thread Q4_K batch-64 GEMM reduced nominal batch tiles but did not survive benchmark adversary; same-process attribution was slightly positive (`164.51 ms` default vs `165.41 ms` off), but matched llama benchmark regressed (`378.72 tok/s` b64 vs `394.94 tok/s` current), so the branch was removed
  - [x] Falsifier: direct Q56_K FFN-down half-output add avoided an explicit `f16_to_f32` buffer but was neutral at pp64 (`167.22 ms` default vs `167.30 ms` branch p50, wins `4/8`), so conversion removal is not enough leverage
  - [x] Falsifier: llama.cpp-style single-buffer Q4_K GEMM with Q4-only `6144` byte threadgroup memory passed focused specs and improved standalone `4096x12288 b64` op attribution (`3.171 ms` → `2.975 ms`), but pp64 wall regressed (`167.39 ms` default p50 vs `176.01 ms` branch p50), so it is not a default prefill win
  - [x] Route tiny Q4_K prefill projections (`out_dim <= 64`, especially recurrent alpha/beta) through GEMV instead of underfilled 64-row GEMM tiles; pp64 paired A/B improves default from `164.04 ms` off to `156.14 ms` on (`8/8` wins), and matched llama comparison moves native prefill to `408.02 tok/s` p50 vs llama `463.3 tok/s`
  - [x] Falsifier: extending the tiny-Q4 GEMV rule to `out_dim <= 1024` makes pp64 slower (`165.86 ms` p50 at threshold 64 vs `169.08 ms` at threshold 1024, wins `8/8` for threshold 64), so keep the rule narrow
  - [x] Falsifier: routing small Q5/Q6 prefill projections (`out_dim <= 1024`) through GEMV instead of batch GEMM is also slower at pp64 (`165.07 ms` default vs `167.05 ms` GEMV, wins `8/8` for default)
  - [x] Disable Crystal GC during multi-token prefill hot path with `QWEN35_PREFILL_GC_GUARD_OFF=1` escape hatch; pp64 paired A/B improves default from `158.00 ms` off to `151.80 ms` on (`8/8` wins), and matched llama comparison moves native prefill to `421.41 tok/s` p50 vs llama `463.04 tok/s`
  - [x] Falsifier: direct Q5_K F32-input/F32-output batch GEMM compiled and passed focused specs, but pp64 paired A/B was neutral/negative (`152.56 ms` default vs `152.52 ms` branch p50, avg worse and wins `3/8`), so Q5 conversion removal is not enough leverage
  - [x] Falsifier: using fast command buffers for prefill helper/group paths compiled and passed specs, but pp64 was neutral (`152.02 ms` default vs `151.97 ms` fast, wins `2/8`) and pp256 slightly worse (`490.49 ms` default vs `490.79 ms` fast), so command-buffer creation is not the remaining gap
  - [x] Remove avoidable host churn in Qwen35 prefill boundaries and Q5/Q6 GEMM staging: shared-buffer reads no longer zero-fill before copy, Q56 zero bias buffers are cleared directly once, and Q56 F16 conversion scratch is reused by size; focused specs pass and pp64 smoke remains around `151.80 ms` p50
  - [x] Falsifier: adding a Q5/Q6 GEMM `no-bias` mode (`apply_gelu=2`) compiled and passed focused specs, but pp64 paired A/B was neutral/negative (`151.84 ms` default vs `151.80 ms` bias path off, wins `3/8`), so the bias add/read is not material enough to justify a kernel mode
  - [x] Add exact Q4_K half-input prefill GEMM (`QWEN35_Q4K_H16_GEMM_OFF=1` disables it): Q4 prefill matmuls preconvert F32 activations to F16 once per matmul instead of per output tile; pp64 paired A/B improves default by `~1.28 ms` (`8/8` wins), pp256 improves by `~5.06 ms` (`6/6` wins), and matched pp64 moves native prefill to `424.96 tok/s` p50 vs llama.cpp `462.9 tok/s`
  - [x] Falsifier: fully pre-dequantized/F16-expanded Q4 FFN weights are not the next prefill lever. A synthetic `4096x12288` F16 GEMM lower-bound was slower than the current compressed Q4_H16 route at b64/b128/b256 (`1.927/3.731/6.872 ms` F16 p50 vs `1.401/2.413/3.132 ms` Q4_H16 p50), so expanding hot Q4 weights to F16 increases traffic more than it removes dequant work.
  - [x] Add exact Q5_K recurrent-qkv half-output prefill path (`QWEN35_Q5_QKV_H16_CONV_OFF=1` disables it): Q5 qkv GEMM output stays F16 and the recurrent conv-shift chunk reads half directly; pp64 paired A/B improves default by `~0.28 ms` (`6/8` wins), pp256 by `~0.52 ms` (`5/6` wins)
  - [x] Reuse Q4_K half-input conversion across paired FFN gate/up matmuls for large prefill batches (`QWEN35_Q4K_PAIR_H16_GEMM_OFF=1` disables it, active at batch `>=256`): pp256 isolated paired A/B improves default by `~1.56 ms` avg / `~2.21 ms` p50 (`7/10` wins), pp512 opt-in A/B improved by `~1.82 ms` (`4/4` pair wins), and pp64 is gated off after adversary A/B showed the pair path is not reliable at short prompts
  - [x] Falsifier: fusing only the `gate/up -> SwiGLU` activation stage is too small to explain the prefill gap. A wait-only `4096x12288` Q4-pair microbench measured `pair_plus_swiglu` almost equal to the pair GEMMs alone at b64/b128/b256 (`1.877/3.268/6.119 ms` vs `1.861/3.255/6.063 ms`), so the remaining WBA lever must fuse a larger FFN diamond or improve the Q4/Q6 matmul kernels themselves.
  - [x] Historical falsifier, later refreshed: an earlier temporary branch sharing one H16 activation conversion between recurrent Q5 qkv and Q4 gate/z projections compiled and passed focused specs, but pp256 paired A/B was flat (`485.56 ms` default vs `485.59 ms` opt-in, wins `4/8`) with the then-current routes. Later kernel/routing changes made the same exact cleanup small-positive enough to retain as `QWEN35_REC_PROJ_SHARED_H16_OFF=1`; treat this old line as stale evidence, not an active refutation.
  - [x] Add conversion-kernel attribution to the prefill profile report so future quiet runs can separate duplicated `F32<->F16` activation/output traffic from quantized weight traffic; smoke pp16 report shows per-row percentages, matmul/conversion logical traffic totals, and mix (`97.50%` matmul / `2.50%` conversion)
  - [x] Add matched benchmark support for exact prompt-cache restore mode (`--native-prefill-cache`): after one seeded prefill, restoring the saved `.qkv` state measures pp64 p50 `52.28 ms` / `1224.28 tok/s` vs llama.cpp `432.72 tok/s` (`+182.93%`) under the same harness; this is a repeated/session prompt-cache speedup, not a first-run prefill replacement
  - [x] Add explicit Metal state-buffer preparation (`Qwen35CPU.prepare_state_metal!` and `--native-prefill-prepare-state`) so server-style benchmarks can exclude first-touch KV/DeltaNet buffer allocation from the timed prefill. pp64 fresh-state prepared mode measured `142.31 ms` / `449.73 tok/s` vs llama.cpp `464.78 tok/s` (`-3.24%`), while default first-run in the same binary measured `149.99 ms` / `426.70 tok/s`.
  - [x] Harden benchmark quiet-mode checks with both per-process and aggregate CPU thresholds (`--load-warning-threshold`, `--load-total-warning-threshold`) so noisy multi-process desktops do not silently pass `--require-quiet`; aggregate warnings now print top CPU contributors
  - [x] Add `--wait-quiet-ms` / `--quiet-poll-ms` to Qwen35 benchmark harnesses so queued A/B runs can wait for a quiet host before measuring instead of immediately aborting or recording noisy timings
  - [x] Guarded baseline refresh: with `--require-quiet --wait-quiet-ms=60000`, pp64 attribution reports p50 `151.73 ms` / `421.80 tok/s`, traffic mix `90.69%` matmul / `9.31%` conversion; matched llama.cpp prompt64/gen64 reports native prefill `426.01 tok/s` p50 vs llama `461.90 tok/s` (`-7.77%`) and native decode `47.60 tok/s` p50 vs llama `45.35 tok/s` (`+4.96%`)
  - [x] Falsifier: removing the inner `simdgroup_barrier(mem_none)` calls from the Q4_K H16 prefill GEMM passed focused Qwen specs (`14 examples, 0 failures`) but regressed guarded pp64 attribution from baseline p50 `151.73 ms` to `154.49 ms`, so keep the barriers
  - [x] Add a guarded paired decode A/B harness for env-tuned wave experiments; guarded trials confirm `QWEN35_WAVE_CHUNK_LAYERS=2` beats unchunked `0` by `~1.34 ms/tok` (`6/6` wins), while `2` vs `4` and `1` vs `2` remain neutral/noisy, so keep default `2`
  - [x] Falsifier: writing Q4 FFN pair input as H16 directly from add+RMSNorm was exact in focused specs, but pp256 paired A/B was neutral (`506.53 ms` default vs `506.29 ms` opt-in, wins `5/10`), so the separate conversion kernel is not the current wall
  - [x] Relaxed-load baseline after CPU-core clarification: sequential pp64 attribution remains stable at p50 `150.80 ms` / `424.39 tok/s`, pp256 at p50 `486.56 ms` / `526.14 tok/s`; matched pp64/gen64 reports native prefill `422.41 tok/s` p50 vs llama.cpp `461.35 tok/s` (`-8.44%`) and native decode `46.59 tok/s` p50 vs llama.cpp `44.38 tok/s` (`+4.98%`)
  - [x] Falsifier: nearby decode scheduler/glue knobs do not recover the missing margin: `QWEN35_WAVE_CHUNK_LAYERS=3` is neutral vs default `2` (`4/8`, delta `+0.013 ms/tok`), `QWEN35_WAVE_FAST_CMD=1` is neutral vs off (`3/6`, delta `-0.018 ms/tok`), `QWEN35_REC_CONVSHIFT_FUSED=1` is slightly worse/noisy (`3/8`, delta `+0.025 ms/tok`), and `QWEN35_DN_POST_FUSED=1` is neutral (`3/8`, delta `-0.014 ms/tok`)
  - [x] Falsifier: matching llama.cpp's Q6_K GEMV `NR0=2` passed focused specs but slowed decode sync profile (`25.67 ms/tok` vs `24.60 ms/tok` old binary in same sequence), so keep local `MV6_NR0=1`
  - [x] Add native Q8_0 draft-model support for Qwen3.5 0.8B: CPU dequant/matmul, Metal GEMV/top1, tied-output fallback when `output.weight` is absent, focused Q8_0 Metal spec, and reusable `--model` sync profiler
  - [x] Add first greedy speculative acceptance harness for Qwen35 target/draft pairs; serial verifier is correctness-only, but establishes acceptance-rate evidence before building a batched verifier
  - [x] Draft baseline: llama.cpp Qwen3.5 0.8B Q8_0 tg64 `145.53 tok/s` (`6.87 ms/tok`), native Q8_0 top1 `11.99 ms/tok`; greedy exact acceptance smoke ranges `13.33%` to `57.14%`, so exact greedy speculative needs either a faster draft, a closer draft, or a sampling-acceptance path before it can beat plain target decode
  - [x] Fix Q8_0 GEMV attribution block accounting and retune Q8_0 GEMV row parallelism; `MV_Q8_NR0=1` improves native 0.8B Q8_0 draft decode from `11.59 ms/tok` to `9.06 ms/tok` in the same 64-token profile, while `NR0=3/4` regress
  - [x] Add exact chunked target top1 verifier primitive (`prefill_tokens_top1s`) and harden shared Metal constant caching for mixed target+draft models; regression spec covers 9B target after 0.8B draft cache pollution
  - [x] Falsifier: chunking only the target verifier body is not enough for speedup with the current 0.8B draft. On `def fibonacci(n):`, gamma=4/tokens=24 has the same acceptance (`85.19%`) but serial verifier is `31.47 ms/tok` while chunk-body verifier is `32.97 ms/tok`; target verification time drops (`503.9` → `471.5 ms`) but fork/copy and per-row lm-head overhead erase it
  - [x] Falsifier: batched Q6_K lm-head top1 for verifier rows is exact but slower at gamma=4. On `def fibonacci(n):`, chunk verifier default is `30.03 ms/tok`; opt-in `QWEN35_HEAD_TOP1_ROWS=1` regresses to `32.44 ms/tok`, so keep row-batched top1 default-off
  - [x] Add exact `chunk-inplace` verifier mode that mutates target state directly and only restores a backup on rejection; on `def fibonacci(n):`, gamma=4/tokens=24 improves verifier wall from serial `29.09 ms/tok` and copy-back chunk `29.13 ms/tok` to `28.36 ms/tok`, but still trails plain target decode (`21.91 ms/tok`)
  - [x] Retune Q8_0 draft GEMV row parallelism from `MV_Q8_NSG=2` to `4` and make Q8 top1 dispatch width follow the Q8 simdgroup count; 0.8B Q8_0 draft decode improves from `~8.88-9.07 ms/tok` to `~8.36-8.38 ms/tok`, while `NSG=8/16/32` are slower or unstable
  - [x] After Q8_0 `NSG=4`, exact speculative `chunk-inplace` on `def fibonacci(n):` improves from `28.36 ms/tok` to `27.70 ms/tok` at the same `85.19%` acceptance, but still trails plain target decode (`21.96 ms/tok`)
  - [x] Add exact Q8_0 FFN gate/up dual GEMV for the 0.8B draft decode wave; it halves FFN-upgate dispatch count and improves interleaved draft decode from `8.54/8.85 ms/tok` with `QWEN35_Q8_DUAL_GEMV_OFF=1` to `8.44/8.51 ms/tok` default, while speculative `chunk-inplace` smoke improves draft time `231.0` -> `223.2 ms` over 32 generated tokens
  - [x] Remove repeated speculative harness rollback allocations by reusing preallocated target/draft backup states and report rollback timing; default prompt `gamma=4/tokens=32` improves from `23.83` to `22.66 ms/tok`, and `gamma=16/tokens=64` proves the high-acceptance path can beat plain target (`15.67` vs `22.24 ms/tok`)
  - [x] Add opt-in adaptive gamma (`--adaptive`, `--max-gamma`) as an exploration harness; with no-regrow-after-reject default, it helps high-acceptance prompts (`19.34 ms/tok` vs plain `22.33`) and avoids the earlier rejection-heavy collapse on `def fibonacci(n):` (`28.55 ms/tok` vs fixed gamma=4 `28.99`, plain target `21.61`)
  - [x] Add exact first-candidate early reject before draft span generation: if `draft_next != target_next`, skip draft backup, skip the rest of the draft candidates, advance one corrected target token, and continue from the corrected draft state. On `def fibonacci(n):`, gamma=4/tokens=64 improves from `32.43 ms/tok` with `QWEN35_SPEC_EARLY_REJECT_OFF=1` to `28.99 ms/tok` default in the same built harness
  - [x] Promote rejection-aware adaptive gamma to the speculative harness default, with `--no-adaptive` / `QWEN35_SPEC_ADAPTIVE=0` for fixed-gamma A/B. In the same built harness, default prompt improves from fixed `23.39 ms/tok` to adaptive `19.30 ms/tok`, and `def fibonacci(n):` improves from fixed `29.59 ms/tok` to adaptive `28.60 ms/tok`
  - [x] Add exact gamma=1 accepted-token fast path for chunk verifiers: when adaptive has fallen to one candidate and `draft_next == target_next`, advance target and draft directly without rollback backups or chunk verifier. On `def fibonacci(n):`, interleaved A/B improved from `27.93/32.02 ms/tok` with `QWEN35_SPEC_SINGLE_FAST_OFF=1` to `27.61/26.72 ms/tok` default
  - [x] Add exact target-only fallback once rejection-aware adaptive drops to low gamma. With `QWEN35_SPEC_PLAIN_FALLBACK_GAMMA=2` default, `def fibonacci(n):` exits speculative after the first rejection and measures `21.57 ms/tok` vs plain target `22.89` in the same final run, while high-acceptance prompt remains faster than plain target (`20.93` vs `23.51 ms/tok`)
  - [x] Raise adaptive `max_gamma` default from `16` to `32` while keeping initial `gamma=4`: final default-prompt smoke is `19.11 ms/tok` vs plain target `21.96`, while `def fibonacci(n):` still falls back after rejection and measures `21.21 ms/tok` vs plain target `21.94`; starting at `gamma=8` was rejected because it regressed `def fibonacci(n):` to `26.56 ms/tok`
  - [x] Re-check Q4_K dual FFN gate/up GEMV as a lower-level decode-kernel branch and reject it again: focused forward spec stayed green (`13 examples, 0 failures`), but paired A/B lost `0.367 ms/tok` (`0/6` wins) versus the separate-GEMV path, confirming this is register-pressure/occupancy bound rather than dispatch-bound
  - [x] Skip draft-state advance/resync before imminent target-only fallback (`QWEN35_SPEC_SKIP_DRAFT_BEFORE_FALLBACK_OFF=1` disables): early-reject `def fibonacci(n):` avoids one unused draft advance, and chunk-reject prompts avoid unused resync (`Once upon a time`: `22.28` vs `22.85 ms/tok`; `The quick brown fox`: `22.56` vs `22.84 ms/tok`); high-acceptance prompt has `draft_skip=0` and stays unchanged within noise
  - [x] Falsifier: deriving a faster local Q4_K_M draft from the downloaded 0.8B Q8_0 GGUF is blocked by llama.cpp (`requantizing from type q8_0 is disabled`); the partial output artifact was removed, so Q4 draft testing needs an F16/BF16 source or a separately downloaded Q4 GGUF
  - [x] Port eager Metal state-buffer preparation into `qwen35_generate` (`QWEN35_PREPARE_STATE_OFF=1` disables). The CLI now prepares target and neural-speculative draft/backup states before latency-sensitive prompt/decode sections, using the same `prepare_state_metal!` path measured in the benchmark harness.
  - [x] Falsifier/tuning note: initial `gamma=3` is not a safe default even though it is faster on the 100%-accept prompt (`~18.3 ms/tok` vs `~19.4`) and mixed partial-reject prompts (`Once upon a time` `21.56` vs gamma4 `22.42`), because it regresses `def fibonacci(n):` to `22.74 ms/tok`; `--max-gamma 16/24` did not recover the high-accept gain, so the effect is the initial chunk schedule, not just the cap
  - [x] Enable exact early-reject and gamma=1 accepted-token fast paths for `--verify serial` too: serial verifier is still not the default for high-accept prompts (`28.83 ms/tok` vs chunk `19.34`), but partial-reject profiling now avoids artificial draft/span waste (`Once upon a time` serial-fast `21.77` vs chunk `22.31`; `The quick brown fox` `21.77` vs chunk `22.41`)
  - [x] Add opt-in `--verify hybrid` mode: first speculative cycle uses serial verifier, later cycles use chunk-inplace. This helps first-cycle partial-reject prompts (`Once upon a time` `21.44` vs chunk `22.47`; `The quick brown fox` `21.73` vs `22.49`) but is not default because high-accept prompt slightly regresses (`19.32` vs `19.08`) and `def fibonacci(n):` regresses (`21.68` vs `21.50`)
  - [x] Falsifier: a progressive verifier that serial-probes 1/2/3 first-cycle candidates and chunks the rest preserved exactness, but it is not worth carrying: high-accept regressed (`19.41-19.70 ms/tok` vs chunk `19.04`), partial-reject prompts were weaker than `hybrid`, and `def fibonacci(n):` regressed (`21.67-21.90` vs chunk `21.51`); temporary code was removed
  - [x] Falsifier: Q8_0 mixed-size dual GEMV for draft recurrent `qkv + gate` lowers `rec.proj` encode accounting (`~3.00 ms` -> `~2.36-2.42 ms` over 16 tokens), but worsens end-to-end draft decode (`~7.85-7.88 ms/tok` separate GEMVs vs `~8.05-8.06 ms/tok` mixed), so this is another dispatch-saving/register-pressure tradeoff rather than a speed path
  - [x] Reuse the existing Q8_0 dual GEMV kernel for draft recurrent `alpha + beta` projections (`QWEN35_Q8_ALPHA_BETA_DUAL_GEMV_OFF=1` disables it): 0.8B draft sync profile improves from `8.77/8.90 ms/tok` off to `8.51/8.43 ms/tok` default over 64 tokens, while focused Qwen specs stay green
  - [x] Skip speculative draft-state backup when a future rejection is guaranteed to enter target-only fallback (`QWEN35_SPEC_SKIP_DRAFT_BACKUP_BEFORE_FALLBACK_OFF=1` disables): exact harness output remains matched to greedy target, and partial-reject prompts avoid the unused draft copy (`draft_backup_skip=1`, `draft_backup=0.0 ms`)
  - [x] Add guarded fast regrowth after adaptive gamma reaches `8` (`QWEN35_SPEC_FAST_REGROW_MIN_GAMMA=0` disables): high-accept prompt now reaches `gamma=32` in `5` cycles instead of `7`, improving `The capital of France is` from `~19.2 ms/tok` to `~16.0-17.8 ms/tok`; `def fibonacci(n):` stays on the safe path (`max_seen=4`, fallback after reject)
  - [x] Reuse the existing Q8_0 dual GEMV kernel for full-attention `K + V` projections (`QWEN35_Q8_KV_DUAL_GEMV_OFF=1` disables): draft full.qkv encode drops from `~3.7 ms` to `~2.7 ms` over 64 tokens and high-accept speculative decode gets a small exact win (`15.73 ms/tok` off -> `15.68/15.71 ms/tok` default)
  - [x] Falsifier: switching fallback-bound low-gamma verifier cycles from `chunk-inplace` to forked `chunk` removes target backup but is not a win; high-accept regresses (`~15.8` -> `~16.0 ms/tok`) and reject prompts are neutral, so keep `chunk-inplace` default
  - [x] Falsifier: raising initial speculative `gamma` from `4` to `5` or `6` is worse under the current fast-regrow schedule; high-accept prompts slow slightly, `def fibonacci(n):` regresses (`gamma=5` verifies one extra reject row, `gamma=6` breaks into many low-gamma cycles), and partial-reject prompts lose acceptance efficiency
  - [x] Falsifier: enabling batched lm-head top1 only for large verifier chunks (`>=8/16/24/32` rows) is still slower than per-row fused top1; high-accept target verifier rises from `~552-556 ms` to `~627-675 ms`, so leave `QWEN35_HEAD_TOP1_ROWS` default-off
  - [x] Refresh: lowering Q4_K pair H16 prefill threshold to b64 is still not a robust pp64 win; it halves FFN upgate conversion accounting but wall remains neutral/noisy (`~156.3-156.6 ms` default vs `~156.1-156.3 ms` branch under host load), so keep pair route gated at batch `>=256`
  - [x] Falsifier: switching Q5_K batch GEMM from the current double-buffered schedule to a llama-style single-buffer schedule is exact but slower on pp64; focused specs pass (`22 examples, 0 failures`), but paired A/B loses (`159.99 ms` default vs `161.55 ms` single-buffer p50, default wins `7/8`), so keep Q5 double-buffering
  - [x] Falsifier: skipping full-attention KV copies in speculative target rollback is exact but not faster; focused rollback spec passed, yet high-accept smoke stayed neutral/slightly worse (`16.30 ms/tok` skip-KV vs `16.20 ms/tok` full backup) and `target_backup` remained `10.6 ms`, showing rollback cost is recurrent state buffers rather than KV
  - [x] Falsifier: Q8_0 GEMV shared-X threadgroup staging is exact but slower for the 0.8B draft; the focused Q8 GEMV spec worsened (`~1.9 ms` -> `24.3 ms`) and draft decode regressed from `8.26` to `9.18 ms/tok`, so current Q8 speed is not limited by repeated x loads
  - [x] Falsifier: Q8_0 block-major weight prepack is not a material GEMV speed path for the 0.8B draft on M2 Max. A real-weight Metal microbench compared the current row-major GGUF block layout with `[block][row]` packed layout and stayed exact (`max_delta=0`), but measured only `~1.009x` on `blk.0.ffn_up 1024x3584`, `~0.999x` on `blk.1.attn_qkv 1024x6144`, and `~1.002x` on `blk.0.ffn_down 3584x1024`; do not add load-time Q8 prepacking unless a different kernel design changes the access pattern.
  - [x] Falsifier: making speculative gamma regrow more aggressive is not a safe default. `QWEN35_SPEC_FULL_ACCEPT_STREAK=1`, `QWEN35_SPEC_FAST_REGROW_MIN_GAMMA=4`, and initial `--gamma 8` all stayed neutral/slower on the 100%-accept prompt (`~16.4-16.9 ms/tok` vs default `16.44`) and regressed `def fibonacci(n):` (`24.75-24.97 ms/tok` vs default `22.98`), so keep the conservative current schedule
  - [x] Add opt-in staged speculative verifier mode for high-accept prompts (`--verify staged --stage-gate 4`). It drafts/verifies a guard stage before the rest of a large gamma cycle and preserves exact greedy output; with fixed `--gamma 32`, high-accept `The capital of France is` improves from default `16.45` to `15.04 ms/tok`, but rejection-sensitive `def fibonacci(n):` regresses to `32.58 ms/tok`, so the mode stays opt-in and not default
  - [x] Falsifier: Q6 lm-head top1 tile rows `12 -> 16` is correctness-safe but not a verifier win; focused specs passed (`13 examples, 0 failures`), yet high-accept target verifier stayed flat (`~573.8 ms` default, `~478-480 ms` staged) and reject default worsened (`22.84 -> 23.45 ms/tok`), so keep rows12
  - [x] Falsifier/fix: full-row batched Q6 lm-head verifier is fast but not exact because its F16 GEMM path can flip close argmax rows (`row24`: exact `220@8.900501` vs F16 full-row `198@8.8984375`). Keep it opt-in with `QWEN35_HEAD_FULL_ROWS=1`; default verifier returned to exact per-row fused top1 and a rows32 ambiguous regression spec now covers this case
  - [x] Fix external speculative verifier fail-closed around guarded full-row target chunks: `qwen35_speculative_accept` now disables `QWEN35_HEAD_FULL_ROWS_GUARDED` inside neural verifier/correction chunks unless `--allow-guarded-verifier` / `QWEN35_SPEC_ALLOW_GUARDED_VERIFIER=1` is explicit. Unsafe guarded mode matched llama.cpp on a high-repeat France prompt (`11.56 ms/tok`, `86.49 tok/s`, `100%` accept) but diverged under `bootstrap32`; the safe exact fix restores parity and a 3-prompt sweep completes without failures (`default` avg `1.153x`, `bootstrap32` avg `1.169x`, with bootstrap bad on code).
  - [x] Port the guarded-verifier fail-closed rule into `qwen35_generate` speculative chunks too; CLI neural speculative verification now disables `QWEN35_HEAD_FULL_ROWS_GUARDED` around chunk verification/correction, matching the exact probe behavior.
  - [x] Add exact `--bootstrap-streak` / `QWEN35_SPEC_BOOTSTRAP_STREAK` policy control, `bootstrap32_s2` sweep preset, and the same env knob in `qwen35_generate`. Requiring two full-accept chunks before jumping to gamma32 avoids the code-prompt collapse from one-shot `bootstrap32`; the 3-prompt gate wins `3/3` vs default (`19.02 ms/tok` avg vs `19.64`, paired ratio `0.962x`), and a broader reps=2 8-prompt gate wins `9/16` with paired ratio `0.979x`.
  - [x] Add `ngram_bootstrap32_s2` sweep preset after exact n-gram front-running proved to be the strongest current policy. On an 8-prompt mix, `--ngram` alone improved paired ratio to `0.853x` vs default, and `ngram + bootstrap32_s2` stayed strong at `0.873x`; France reaches `~10.1-11.3 ms/tok`, faster than the local llama.cpp speculative run (`~11.75 ms/tok`) on the same target/draft pair.
  - [x] Add exact Q5_K/Q6_K prefill GEMM f32-output kernels that preserve the previous half-rounded output contract but skip the separate half-output → f32 conversion dispatch; also vectorize Q4_K H16 input tile loads. Focused specs pass (`23 examples, 0 failures`), standalone pp64 op attribution drops measured weighted hot-shape lower bound from `8.209 ms` to `2.913 ms`, while full pp64/pp256 wall remains host/noise limited around `153 ms` / `494 ms`
  - [x] Add matched benchmark mode for preallocated prompt state (`--native-prefill-prealloc`): it allocates GPU state once, resets only recurrent conv/SSM state between reps, and preserves exact `start_pos=0` output (`fresh=72:9.439041`, `reuse=72:9.439041`). Prompt64/gen64 smoke improves native prefill from `165.90 ms` p50 default to `156.27 ms` p50 preallocated, moving the matched gap from `-3.10%` to `-1.57%`; this is session-style state reuse/harness attribution, not a first-run kernel breakthrough.
  - [x] WBA/LTP next-frame saved: Qwen35 already uses wave scheduling, so the next breakthrough target is not "add WBA" but lift WBA from command-buffer groups to algebraic operator diamonds. Prime candidate is FFN `normed -> gate/up -> SwiGLU -> down -> residual` as a tile-streamed exact diamond, plus speculative verifier as a time-axis WBA batch.
  - [x] Falsifier: extending the existing in-place SwiGLU activation trick from prefill into decode wave is correctness-safe but not a default win. `QWEN35_DECODE_SWIGLU_INPLACE=1` passes targeted specs (`17 examples, 0 failures`) but paired decode A/B loses/noisy versus default (`25.135 ms/tok` default vs `25.786 ms/tok` opt-in; default wins `5/8`), so keep it default-off and treat full tile-streaming FFN as the real diamond, not buffer aliasing.
  - [x] Promote exact row-batched verifier top1 only for large chunks (`QWEN35_HEAD_TOP1_ROWS_MIN`, default `8`; `QWEN35_HEAD_TOP1_ROWS_OFF=1` disables): targeted specs pass (`17 examples, 0 failures`), high-accept speculative A/B wins `4/4` against rows-off (`16.29/21.04/21.13/19.90 ms/tok` auto vs `17.63/21.30/22.10/23.93 ms/tok` off), while rejection-heavy paths with `gamma=4` stay below the threshold and use the old per-row exact top1 path.
  - [x] Falsifier: making the old full-row Q6 lm-head route use f32 output/reduce still does not make it exact. Focused specs passed (`17 examples, 0 failures`), but live greedy equivalence diverged on `The capital of France is` at token 17 (`plain ... 13 ...` vs speculative `32` after the f32-output attempt), so no code was retained and `QWEN35_HEAD_FULL_ROWS=1` remains an unsafe upper-bound/research knob rather than a verifier path.
  - [x] Falsifier: adaptive staged verifier and regrow scheduling are not default wins after row-batched top1. `--verify staged --stage-gate 8/16` regresses the high-accept prompt (`18.76 ms/tok` chunk-inplace vs `26.86/21.79`) and does not help rejection-heavy prompts; `QWEN35_SPEC_ADAPTIVE_REGROW=1` reintroduces expensive draft/resync work and regresses `def fibonacci(n):` (`23.77 -> 48.00 ms/tok`).
  - [x] Falsifier: naive prefix-copy of Metal KV buffers in `State.copy_from!` is not safe with the current state-position invariant. Copying only `position / max_seq` bytes broke focused verifier specs (`chunked top1 verifier` and `target/draft constants` logit mismatches), so state copy stays full-buffer until KV valid-length is explicitly tracked per layer/buffer.
  - [x] Add opt-in speculative bootstrap jump (`--bootstrap-gamma N` / `QWEN35_SPEC_BOOTSTRAP_GAMMA`): on 100%-accept `The capital of France is`, bootstrap32 cuts cycles `5 -> 3` and improves `17.89 -> 15.10 ms/tok`; it remains default-off because `def fibonacci(n):` regresses (`23.36 -> 29.93`) after an initially accepted prefix.
  - [x] Fresh matched llama.cpp benchmark: with preallocated native prompt state, pp64 prefill beats llama.cpp by `+35.53%` (`335.25 tok/s` p50 native vs `247.37 tok/s` llama.cpp); decode is effectively parity/slightly ahead at `+1.03%` (`16.32 tok/s` p50 native vs `16.15 tok/s` llama.cpp).
  - [x] Falsifier: approximate draft layer truncation is not a useful exact speculative speed path for the current 0.8B Q8_0 draft. The draft has `24` layers, so `--draft-layers 24/26/28` is the full model; the first real truncation tested (`22`) collapses high-accept prompts to immediate reject/fallback (`The capital of France is`: `100%` full-draft accept at `~15.55 ms/tok` vs `0%` accept and `~20.63 ms/tok` with 22 layers), while partial-reject prompts also lose acceptance.
  - [x] Falsifier: lowering exact row-batched verifier top1 threshold from `8` rows to `4` is not a safe default. A noisy but adversarial four-prompt smoke showed mixed results: `def fibonacci(n):` improved in that run (`32.61 -> 30.91 ms/tok`), but high-accept `The capital of France is` regressed (`19.58 -> 22.60 ms/tok`) and `The quick brown fox` regressed (`31.08 -> 33.63 ms/tok`), so keep `QWEN35_HEAD_TOP1_ROWS_MIN=8`.
  - [x] Add exact n-gram/cache speculative harness (`bin/qwen35_ngram_speculative.cr`) as a non-neural draft paradigm. It keeps target verification exact and proposes candidates only after a repeated suffix of at least 6 tokens; high-repeat `The capital of France is` accepts `55/55` n-gram candidates and measures `17.85 ms/tok` in the smoke, while `def fibonacci(n):` proposes no candidates and falls back to plain target steps instead of burning verifier chunks on weak 1-grams.
  - [x] Extract n-gram draft selection into reusable `ML::GGUF::NgramDraft` with focused specs, so the next step can compose n-gram draft with the neural Q8_0 draft without duplicating suffix-matching logic.
  - [x] Add recursive n-gram candidate extension: once a repeated suffix is found, the draft can extend through its own scratch history up to `gamma=32` while still being target-verified exactly. High-repeat `The capital of France is` improves from non-recursive `~17.46 ms/tok` / `7` verifier cycles to recursive `~9.0 ms/tok` / `2` cycles; no-repeat `def fibonacci(n):` still proposes no candidates and falls back to plain target steps.
  - [x] Add n-gram fail-closed guard: after the first rejected n-gram chunk, the harness disables further n-gram drafting by default (`--keep-ngram-after-reject` / `QWEN35_NGRAM_DISABLE_AFTER_REJECT_OFF=1` restores exploration). This preserves the high-repeat win and avoids repeated verifier waste on partial-repeat prompts.
  - [x] Compose n-gram/cache draft with the neural speculative harness behind `--ngram` / `QWEN35_SPEC_NGRAM=1`. The n-gram front-runner is exact target-verified, lazy-resyncs the Q8_0 draft only if neural drafting is needed afterward, and improves high-repeat `The capital of France is` from neural-only `17.08 ms/tok` to n-gram+neural `11.24 ms/tok` in the same built harness while leaving no-repeat adversary prompts effectively unchanged.
  - [x] Falsifier: target-only seeding before the first n-gram match is unsafe with the current chunk verifier. It improves high-repeat `The capital of France is` to `~8.85 ms/tok`, but adversary `The quick brown fox` diverges from greedy at token 31 even when row-batched top1 is disabled for the n-gram chunk, so no target-seed code was retained.
  - [x] Add opt-in exact n-gram speculative decode to the practical generation CLI (`QWEN35_NGRAM_DECODE=1`). `qwen35_generate` now reuses `ML::GGUF::NgramDraft`, verifies chunks with the target path, fails closed after first n-gram rejection by default, and preserves exact greedy token IDs on both high-repeat `The capital of France is` (`55/55` n-gram accepted) and adversary `The quick brown fox` (`18/32` accepted then disabled).
  - [x] Add opt-in exact neural speculative decode to the practical generation CLI (`QWEN35_SPECULATIVE_DECODE=1`, optional `QWEN35_DRAFT_MODEL`). It reuses the target chunk-inplace verifier, adaptive gamma, early reject, and fallback policy; 32-token parity smokes match greedy output on high-accept and rejection prompts, while 64-token high-accept generation improves from `20.40 ms/tok` greedy to `16.61 ms/tok` neural speculative and `15.10 ms/tok` with guarded full-row verification.
  - [x] Falsifier/fix: composing n-gram and neural speculative inside `qwen35_generate` is not a speed win and exposed an exactness hazard when guarded full-row verification is applied to partial n-gram rejects. The retained fix is fail-closed: n-gram verifier chunks temporarily disable `QWEN35_HEAD_FULL_ROWS_GUARDED`, preserving greedy parity even if the env is set; ngram+neural state-machine composition was not retained.
  - [x] Apply the same fail-closed n-gram verifier guard to the research speculative harness (`bin/qwen35_speculative_accept.cr`): n-gram chunks now temporarily disable `QWEN35_HEAD_FULL_ROWS_GUARDED`, while neural chunks can still use the guard. Focused forward specs pass, high-repeat n-gram+guard smoke stays exact at `9.90 ms/tok`, and partial-repeat `The quick brown fox` stays exact with n-gram disabled after first reject.
  - [x] Fix thresholded row-batched verifier activation: `prefill_tokens_top1s` now always asks the routed output-head helper, which auto-enables exact row-batched top1 only for chunks at or above `QWEN35_HEAD_TOP1_ROWS_MIN=8` and still respects `QWEN35_HEAD_TOP1_ROWS_OFF=1`. Current smoke on `The capital of France is` improves high-accept speculative wall from `17.0 ms/tok` with rows-off to `16.43-16.67 ms/tok` default; `def fibonacci(n):` and `The quick brown fox` stay exact and remain on the gamma-4/per-row fallback path.
  - [x] Add opt-in guarded full-row verifier accelerator (`QWEN35_HEAD_FULL_ROWS_GUARDED=1`): large verifier chunks run the fast full-row Q6/F16 path, compute top1/top2 margins, and exactly fall back through row-batched fused top1 for low-margin rows. It is not default because accepted fast rows still rely on the F16 margin guard, but exact harness smokes passed; high-accept `The capital of France is` improved to `14.32 ms/tok`, and n-gram+neural improved to `9.20 ms/tok` in fresh local runs.
  - [x] Add the first exact FFN WBA sub-diamond in decode wave: Q4_K/Q6_K FFN-down GEMV can now fold the final residual add into the GEMV output write (`QWEN35_FFN_DOWN_ADD_FUSED_OFF=1` disables). Focused specs pass, default-vs-off token IDs match on `The quick brown fox`, sync profile removes `rec.add/full.add` trace kernels and improves the short smoke (`24.82 ms/tok` default vs `29.57 ms/tok` off), while paired A/B under noisy load is small/neutral-to-positive (`+0.067..0.312 ms/tok`).
  - [x] Extend the exact FFN-down residual-add WBA path to Q8_0 draft GEMV: the 0.8B draft wave now emits `rec.ffn_down_add/full.ffn_down_add` instead of `ffn_down + add`. Focused Metal/forward specs pass, and paired 0.8B draft A/B improves from `8.257 ms/tok` off to `8.102 ms/tok` default (`7/8` wins), though full speculative wall is still target-verifier dominated.
  - [x] Add an env-tunable Q4 pair-H16 prefill threshold (`QWEN35_Q4K_PAIR_H16_MIN_BATCH`). Historical default `256` avoided noisy pp64/pp128 evidence, but after later H16 routing cleanups a refreshed threshold sweep supports default `64`: threshold64 beats threshold256 at pp64 (`150.05 ms` vs `150.38 ms`, `12/12` wins in the repeat) and pp128 (`263.66 ms` vs `264.07 ms`, branch wins `5/6`); default-vs-off remains small-positive (`pp64 -0.13 ms`, `5/8`; `pp128 -0.57 ms`, `5/6`).
  - [x] Falsifier: lowering `QWEN35_Q4K_PAIR_H16_MIN_BATCH` from `64` to `32` is not a useful short-prompt win. Prepared-state A/B was neutral at pp32 (`92.42` vs `92.40 ms`, wins `2/6`) and pp64 (`142.95` vs `142.87 ms`, wins `1/6`), and slightly worse at pp128 (`256.28` vs `256.50 ms`, wins `5/6` for default), so keep the current threshold at `64`.
  - [x] Falsifier/default-off feature: ported the same FFN-down residual-add WBA pattern to prefill chunk routes for Q6_K batch GEMM (`QWEN35_PREFILL_FFN_DOWN_ADD_FUSED=1` enables). Correctness specs pass in both default and opt-in modes, but paired prefill A/B does not justify default-on: pp64 regressed in the original lazy-state run, and a refreshed prepared-state check is neutral/negative at pp64 (`142.32 ms` default vs `142.43 ms` opt-in, `4/8`) and pp256 (`477.15 ms` default vs `478.33 ms` opt-in, `4/6`). Earlier Q4 batch-add attempts were rejected because cooperative stores lost the direct simdgroup-store advantage, and an accidental Q4 batch-to-GEMV route catastrophically slowed pp64/pp256; keep the safe Q6-only path opt-in only as a controlled experiment.
  - [x] Falsifier: caching the four per-lane `DeltaNet` post-norm `Y[d]` values in registers removes a second global read in theory, but it is not a wall win in the current post kernel. Focused Metal specs pass, yet pp64 worsened (`152.56 ms` old p50 vs `155.71 ms` patched p50) and pp256 was neutral (`492.55 ms` old vs `492.23 ms` patched), so the patch was removed; the next recurrent breakthrough must fuse the DeltaNet/post boundary at the scan level or change the scan algorithm, not just local-cache the post pass.
  - [x] Add explicit practical CLI decode policy (`QWEN35_DECODE_POLICY=greedy|ngram|speculative|auto`) and fail-fast legacy conflict handling. `auto` is intentionally conservative and maps to the exact fail-closed n-gram path for now; real CLI parity checks against greedy pass for `The capital of France is`, `The quick brown fox`, and `def fibonacci(n):` at 32 generated tokens, while setting both legacy mode envs without an explicit policy now errors before model load instead of silently choosing neural speculative.
  - [x] Falsifier: extending FFN-down residual-add fusion into the `full_attn_layer_chunk_project_last` prefill-final path is correctness-safe but not a speed win. Focused Qwen/Metal specs passed (`23 examples, 0 failures`), yet pp64 paired A/B slightly favored the old separate `ffn_down + add` route (`150.64 ms` fused vs `150.45 ms` old, fused wins `3/8`), so no source change was retained.
  - [x] Reduce exact n-gram draft overhead on no-candidate paths: recursive mode now probes the current history once and only duplicates/extends scratch history after a first match exists. Focused n-gram specs pass, an isolated 2048-token no-match microbench improves slightly (`370.43 -> 358.58 ms` over 5000 calls), and real `QWEN35_DECODE_POLICY=auto` parity still matches greedy on the three 32-token CLI smoke prompts.
  - [x] Port the harness-only speculative bootstrap jump into the practical generation CLI (`QWEN35_SPEC_BOOTSTRAP_GAMMA=N`, default off). It remains opt-in because earlier harness evidence showed high-accept speedups but regressions on prompts that reject after an accepted prefix; CLI parity smokes with bootstrap32 still match greedy token IDs on high-accept and rejection prompts.
  - [x] Port exact draft-backup/resync skip before target-only fallback into `qwen35_generate` (`QWEN35_SPEC_SKIP_DRAFT_BEFORE_FALLBACK_OFF=1` and `QWEN35_SPEC_SKIP_DRAFT_BACKUP_BEFORE_FALLBACK_OFF=1` disable). Practical CLI parity matches greedy/default-off token IDs on high-accept and rejection prompts; the partial-reject smoke improves `The quick brown fox` from `25.99 -> 25.09 ms/tok` by avoiding one draft resync, while high-accept is neutral/noisy.
  - [x] Falsifier/narrowing: raising neural speculative `QWEN35_SPEC_PLAIN_FALLBACK_GAMMA` from default `2` to `8` is not a robust default win. It helps some `bootstrap_gamma=16` cases by skipping draft backups and falling back sooner after rejection (`The capital of France is` around `15.37 ms/tok`; `def fibonacci(n):` around `21.38 ms/tok` instead of `24.5` with fallback `2`), but no-bootstrap high-accept was neutral/noisy and one adjacent run regressed (`17.67` default-8 vs `17.50` old-2). Keep default `2`; use `fallback_gamma>=8` only with explicit bootstrap/high-accept tuning.
  - [x] Falsifier: disabling auto n-gram after 16 consecutive no-candidate plain steps preserves exact output but is not a useful speed policy. It leaves high-repeat behavior intact, but no-repeat `def fibonacci(n):` stayed slightly slower (`20.29 ms/tok` with miss-limit vs `20.22 ms/tok` without) and the policy can suppress late n-gram wins, so no source change was retained.
  - [x] Add the harness-compatible `QWEN35_SPEC_PLAIN_FALLBACK_OFF=1` escape hatch to `qwen35_generate` without changing defaults. The CLI now reports whether target-only fallback is enabled, and the skip-draft-before-fallback optimization is gated by the same fallback policy so fallback-off A/B remains semantically clean.
  - [x] Make the practical CLI n-gram rollback backup state lazy: `qwen35_generate` now allocates the backup only when a candidate chunk actually appears. This is exact and avoids a large unused state allocation on no-candidate auto/ngram runs; parity still matches greedy for high-repeat, partial-repeat, and no-repeat 32-token smokes, though wall timing is neutral/noisy.
  - [x] Add default-preserving CLI trace suppression (`QWEN35_TRACE_STEPS_OFF=1` or `QWEN35_QUIET=1`) for benchmark/generation runs. Summaries and final output stay visible, but per-token/per-cycle logs are suppressed; greedy, auto, and neural speculative 32-token parity smokes still match baseline token IDs on high-repeat, partial-repeat, and no-repeat prompts.
  - [x] Port the harness gamma=1 accepted-token fast path into `qwen35_generate` (`QWEN35_SPEC_SINGLE_FAST_OFF=1` disables). The practical CLI now bypasses rollback backup and chunk verification when `draft_next == target_next` at `gamma=1`, preserving exact token IDs; default target-only fallback means the path is mostly an A/B/control win for fallback-off speculative runs rather than a default speed lever.
  - [x] Align the neural speculative acceptance harness with practical CLI state setup: `bin/qwen35_speculative_accept.cr` now prepares target/draft/backup Metal state buffers by default and reports `prepare_state=...`; `QWEN35_PREPARE_STATE_OFF=1` preserves the old lazy-allocation path for A/B. High-accept 16-token smoke stays exact and reduces backup/copy timing noise (`target_backup ~2.2 ms`, `draft_backup ~0.4 ms` prepared vs `~5.3/~1.8 ms` lazy in the adjacent run).
  - [x] Fresh speculative attribution: high-accept neural speculation with bootstrap32/full-row guard reaches `11.49 ms/tok` for 64 generated tokens (`87.04 tok/s`, `100%` accepted), but time is now draft-dominated (`draft=451.3 ms`, `target_verify=274.6 ms`). Partial-reject `The quick brown fox` correctly falls back after one cycle and is parity/slightly slower than plain target (`22.33` vs `22.11 ms/tok`), so the next exact neural-spec win needs a faster/smaller draft or fewer draft steps, not just verifier micro-tuning.
  - [x] Falsifier: reducing Q8_0 top1 rows-per-threadgroup from `12` to `8` looked faster in draft sync profile (`~7.31-7.44 ms/tok` vs `~7.65-7.74 ms/tok` on repeated 64-token draft runs), but failed exact speculative parity on `The capital of France is` with bootstrap32/full-row guard. The source was reverted to `12`; Q8 top1 tile-shape changes require end-to-end speculative parity, not just draft-only speed.
  - [x] Falsifier: fusing Q8_0 dual FFN gate/up GEMV with SwiGLU activation removed the `ffn_act` trace rows but regressed draft wall. Focused specs passed with `QWEN35_Q8_FFN_SWIGLU_FUSED=1`, yet paired 64-token draft profiles favored the existing separate activation path (`~7.73-7.81 ms/tok` off vs `~7.75-8.18 ms/tok` fused). The temporary kernel/route was removed; like the earlier Q4 pair+SwiGLU branch, putting `exp` into the projection kernel is not a speed path.
  - [x] Add practical CLI neural verifier selection (`QWEN35_SPEC_VERIFY=chunk-inplace|hybrid|serial`). Default remains `chunk-inplace`; opt-in `hybrid` verifies the first speculative cycle serially and then returns to chunking, improving first-cycle partial-reject smokes (`Once upon a time` `22.84/23.06 -> 22.48/22.11 ms/tok`, `The quick brown fox` `22.86/22.85 -> 22.12/22.22`) while regressing high-accept prompts (`The capital of France is` `16.33/16.40 -> 16.94/16.72`).
  - [x] Falsifier: a Q8_0 mixed dual GEMV for unequal recurrent draft projections (`qkv 1024x6144` + `gate 1024x2048`) reduced `rec.proj` encode time but regressed 0.8B draft wall (`~7.39-7.47 ms/tok` separate vs `~7.62-7.70` mixed), so the temporary kernel/route was removed. Dispatch fusion alone is not enough for this Q8 projection shape.
  - [x] Add exact recurrent prefill shared-H16 projection input: Q5 qkv and Q4 gate GEMMs now share one F32→F16 conversion (`QWEN35_REC_PROJ_SHARED_H16_OFF=1` disables). Focused Qwen/Metal specs pass (`23 examples, 0 failures`); paired A/B is neutral at pp64 (`150.45 ms` default vs `150.55 ms` off, `4/8` wins) and small-positive at pp256/pp1024 (`483.28 vs 484.48 ms`, `4/6`; `1916.54 vs 1917.83 ms`, `3/3`) while reducing profiled conversion traffic by one recurrent `4096*b` conversion per recurrent layer.
  - [x] Re-check llama.cpp Q4_K prefill GEMM source against the current local Q4_H16 route. llama.cpp still uses the same 64x32/NK32/4-simdgroup simdgroup-matrix `kernel_mul_mm_q4_K_f32` family; the local route already keeps the same tile shape and adds prepared F16 input reuse. Prepared-state pp64 A/B confirms the H16 route is still material (`145.99 ms` default vs `148.07 ms` with `QWEN35_Q4K_H16_GEMM_OFF=1`, `6/6` wins), so there is no obvious llama.cpp Q4 tile trick left to copy.
  - [x] Add opt-in intra-group prefill phase profiling (`QWEN35_PREFILL_PHASE_PROFILE=1`) for `full+rec` grouped command buffers. It intentionally inserts profiling command-buffer boundaries after the full-attention layer and after each recurrent layer, so it is attribution-only, not a production route. pp64 prepared profile shows each `full+rec` group is almost exactly four similar phases: full layer `~4.39-4.46 ms`, each recurrent layer `~4.50-4.61 ms`; default env-off profile remains unchanged around `142.96 ms` p50. This refutes a hidden single bad phase inside the grouped wave.
  - [x] Falsifier: exact compact DeltaNet summaries do not currently beat the production rowwise chunk even as a final-state lower bound. `qwen35_deltanet_compact_vs_rowwise_micro` measured compact vs rowwise p50 at block8 `0.3375` vs `0.2665 ms`, block16 `0.4396` vs `0.2831 ms`, and block32 `0.3536` vs `0.2636 ms`; row-threadgroup apply was also slower/neutral. Row-basis summary composition itself is exact and tileable, but the block-scan model says rank growth/composition cost only becomes attractive for long prompts with a much better integrated compact scan. Do not integrate compact summaries into first-run prefill yet.
  - [x] Falsifier: Q4_K per-subblock 16-entry dequant lookup tables are exact but slower. A temporary `dequantize_q4_K_fn` LUT variant passed focused Qwen/Metal specs (`23 examples, 0 failures`), but hot-shape attribution for Q4 `4096x12288 b64` worsened from `1.600 ms` after reverting to the current arithmetic path to `3.748 ms` with the LUT; the smaller Q4 `4096x4096 b64` shape also worsened (`0.661 ms` current vs `0.753 ms` LUT). Do not add nibble lookup/precompute inside the current Q4 GEMM kernel; register/local pressure dominates the saved multiply.
  - [x] Falsifier: changing Q4_K GEMM dequant arithmetic from float to half is neutral and weakens numeric margin. A temporary half-arithmetic `dequantize_q4_K_fn` passed focused specs, but `metal_q4k_gemm` max diff increased to `0.0012678653`; hot-shape attribution was effectively flat on Q4 `4096x12288 b64` (`3.032 ms` half vs `3.067 ms` reverted current) and worse on Q4 `4096x4096 b64` (`0.164 ms` half vs `0.147 ms` current). Keep the current float expression with one final half rounding.
  - [x] Falsifier: removing inner `simdgroup_barrier(mem_none)` calls from Q5/Q6 f32-output prefill GEMM kernels is not a win. A temporary barrier-free Q5/Q6 patch passed focused specs (`23 examples, 0 failures`), but spec timings showed large regressions (`metal_q5k_gemm` `176.8 ms`, `metal_q6k_gemm` `45.1 ms`), and hot-shape attribution worsened Q6 `12288x4096 b64` to `0.897 ms` and Q5 `4096x8192 b64` to `0.503 ms` versus current baselines around `0.757/0.414 ms`. Keep the barriers, matching the earlier Q4 barrier falsifier.
  - [x] Falsifier: resynchronizing the neural draft immediately after a CLI early-reject is not a speed win when target-only fallback is disabled. A temporary `qwen35_generate` patch made fallback-off `def fibonacci(n):` acceptance jump from `4/32` to `31/32`, but wall regressed from `23.63` to `27.36 ms/tok`; `Once upon a time` similarly regressed from `28.02` to `33.04 ms/tok`. Keep the current fail-cheap behavior: after low-confidence early rejects, spending draft cycles to improve acceptance can be slower than letting target steps dominate.
  - [x] Add `bin/qwen35_speculative_sweep.cr`, a measurement-only multi-prompt policy sweep for the speculative acceptance harness. It runs a compiled `qwen35_speculative_accept` runner across named policies (`default`, `guard`, `bootstrap32`, `bootstrap32_guard`, `hybrid`, `ngram`, `ngram_guard`), parses `ms/tok`, acceptance, gamma stats, and plain-target baseline, then prints per-prompt rows plus policy averages. The sweep now interleaves policies inside each prompt/repetition block to reduce host-drift bias; an interleaved smoke with `--tokens 8 --reps 2 --policies default,bootstrap32,ngram` ranked default and n-gram near parity (`1.274x`/`1.267x`) while bootstrap32 lagged (`1.213x`), demonstrating why policy changes need multi-prompt wall-time sweeps instead of one-prompt acceptance claims.
  - [x] Add `--dump-cycles JSONL` to the neural/ngram speculative harness. `bin/qwen35_speculative_accept.cr` now writes per-cycle JSONL with target/draft model ids, prompt hash, position, policy/kind, verifier mode, gamma, proposed/accepted counts, reject index, n-gram match features, timing breakdowns, candidate hash, and post-run `expected_gain_ms` versus plain target ms/token. Raw token ids are omitted by default and require explicit `--dump-cycle-token-ids` / `QWEN35_SPEC_DUMP_TOKEN_IDS=1`. Verified with high-accept neural, token-id opt-in, and forced n-gram-cycle smokes.
  - [x] Fix speculative harness baseline timing: `plain_target_wall` is now target decode-only after its own prompt prefill, matching `spec_wall`, and `plain_target_prefill_wall` is printed separately. Previous `qwen35_speculative_accept` speedup and `expected_gain_ms` numbers that used prefill-included plain baselines are stale; parity/acceptance counts remain useful.
  - [x] Wire cycle dumps into the multi-prompt policy sweep: `bin/qwen35_speculative_sweep.cr --dump-cycles-dir DIR` now forwards per-run `--dump-cycles` paths named by rep/prompt/policy, optionally forwards `--dump-cycle-token-ids`, and reports dump file counts. A 2-prompt/2-policy smoke produced 4/4 JSONL files, including both neural and n-gram cycle records.
  - [x] Add cycle-level router diagnostics to the sweep output. When `--dump-cycles-dir` is active, the sweep parses produced JSONL files and prints aggregate `policy/kind` cycle counts, accepted/proposed totals, reject counts, summed expected gain, and timing totals. A smoke summarized `neural/neural` and `ngram/ngram` rows separately, giving immediate signal for router feature work without a separate notebook.
  - [x] Add prompt labels and a privacy-preserving manifest to cycle-dump sweeps. `--prompt` / `--only-prompts` entries can now use `NAME::TEXT`; dump filenames include the safe prompt label, and `prompt_manifest.jsonl` records `prompt_index`, `prompt_name`, prompt hash, and byte length. Raw prompt text remains opt-in via `--dump-prompt-texts`; default sweep output now prints labels instead of raw prompt previews.
  - [x] Add named warm-aware gamma policies to the sweep: `router16` (`gamma4`, cap `max_gamma=16`, jump to `16` after one full accept), `fixed16`, and n-gram variants. These are measurement aliases over existing exact verifier controls, not new decode semantics. Fake-runner and real-runner smokes verified policy selection, labeled dump filenames, and cycle summaries.
  - [x] Add named staged large-gamma policies to the sweep: `staged16` and `ngram_staged16` use fixed `gamma16` with `--verify staged --stage-gate 4`, so the verifier can stop after a bad first stage instead of always paying for a full wrong tail. On the 27B+2B `gen16` four-prompt gate, staged improved low-accept prompts relative to fixed16 (`France` `0.434x -> 0.680x`, `quick_fox` `0.563x -> 0.607x`) but regressed high-accept prompts relative to full chunk; `ngram_staged16` was near default by paired delta (`+0.66ms/tok`) and still needs a router.
  - [ ] Build a local acceptance/gain dataset for 27B target with 0.8B and 2B drafts. Start with `200-500` prompts across code, chat/facts, repetition/templates, JSON/markdown/logs, and known adversaries (`The capital of France is`, `Once upon a time`, `The quick brown fox`, `def fibonacci(n):`). Labels are generated automatically by exact target verification: `accepted_count`, `accept>=3/4`, and `expected_gain_ms > 0`.
    - [x] Dataset preflight: local 27B target (`Qwen3.6-27B-Q4_K_M`) is vocab-compatible with both local drafts (`Qwen3.5-0.8B-Q8_0` and `Qwen3.5-2B-Q4_K_M`). Short `--tokens 2 --gamma 2 --dump-cycles` smokes on `The capital of France is` preserved greedy parity and wrote JSONL labels for both drafts. This only verifies compatibility/data path, not broad acceptance quality.
    - [x] Corrected 27B router-data mini-gate: 4 prompts (`France`, forced repetition, `def fibonacci`, `quick brown fox`), `tokens=4`, policies `default,ngram`, exact parity preserved through the runner. With decode-only plain baseline, both external drafts are slower than target-only decode on this narrow slice; 2B still beats 0.8B on speculative wall and acceptance (`default`: `92.62ms/tok`, `87.5%` vs `102.93ms/tok`, `81.25%`; `ngram`: `83.76ms/tok`, `87.5%` vs `97.56ms/tok`, `81.25%`). Treat this as router-data and a verifier-cost falsifier, not a speed win.
    - [x] Verifier route/gamma falsifier: on a 27B + 2B high-accept `def fibonacci` prompt, `chunk-inplace` is the best current verifier mode but still slower than target-only decode. At `tokens=8`, fixed `gamma=4/8/16` with `100%` acceptance measured `96.30/73.56/68.23ms/tok` versus plain decode around `56ms/tok`; target verifier alone was `677.8/507.4/466.5ms` before draft cost. External full-draft speculation cannot be the next speed path until verifier chunks beat serial decode or draft work is much cheaper/hidden.
    - [x] Add default-on target verifier warmup in the research acceptance harness. It warms `prefill_tokens_top1s` on a forked target state before decode timing and reports `verifier_warmup_wall` separately; `--no-warm-verifier` / `QWEN35_SPEC_WARM_VERIFIER_OFF=1` preserve cold A/B. This converts 27B high-accept `def fibonacci` `gamma16/tokens8` from slower-than-plain to faster (`0.8B`: `47.17ms/tok` vs `56.31`; `2B`: `48.24ms/tok` vs `56.18`), but the warmup itself costs about `417-459ms`, so it is a throughput/long-run lever, not a first-token-latency win.
    - [x] Warm-aware gamma gate: fixed `gamma16/tokens8` with 2B draft wins high-accept prompts but loses the 4-prompt mix (`default 0.849x`, `ngram 0.909x`) because `France` and partial `quick_fox` rejects dominate. Adaptive `gamma4->16` is safer but still sub-break-even mixed (`default 0.929x`, `ngram 0.985x`), and bootstrap16-after-one-full-accept is effectively flat (`default 0.922x`, `ngram 0.995x`) at this short length. Router policy must avoid low-accept chunks and reserve large gamma for longer/high-confidence spans.
    - [x] Longer `gen16` warm-router gate: on 27B+2B over `France`, repeated alpha, `def fibonacci`, and `quick_fox`, `ngram_router16` was the best measured policy by paired delta (`-4.40ms/tok` vs default, wins `3/4`) and strong on repeated/code prompts (`2.727x`, `1.486x`). It is still not a default: `quick_fox` regressed (`0.647x`), and fixed `gamma16` was unsafe despite high-accept wins because low-accept prompts dominate tail cost. Next router needs an explicit first-chunk risk gate before enabling large-gamma continuation.
    - [x] Add reusable staged-router sweep aliases after the extra-args experiment: `router16_staged` and `ngram_router16_staged` combine the warm `gamma4->16` router with `--verify staged --stage-gate 4`. Fake-runner arg capture verified the forwarded flags, a 9B smoke preserved runner compatibility, and a 27B+2B no-dump `gen16` 4-prompt gate put `ngram_router16_staged` at paired delta `-5.04ms/tok` versus default, wins `3/4`, average ratio `0.889x`. It remains research-only: repeated alpha is the main gain (`2.65x`), code is only slightly faster (`1.138x`), and low-accept `quick_fox` still regresses (`0.776x`).
    - [x] Add `bin/qwen35_spec_router_dataset.cr`, a local JSONL extractor for learned-router training rows. It reads sweep `--dump-cycles-dir` outputs plus `prompt_manifest.jsonl`, reconstructs `sweep_policy`, adds derived labels (`accepted_ratio`, `full_accept`, `accept_ge_75pct`, `positive_gain`), preserves privacy by using prompt labels/hashes rather than raw prompt text, and prints policy/kind summaries. Smokes on the repeat-alpha and 4-prompt dump dirs produced `8` and `77` rows respectively.
    - [x] Add `bin/qwen35_spec_router_train.cr`, an offline logistic-regression baseline for router rows. On the tiny current combined dataset (`85` rows from repeat-alpha plus the 4-prompt staged-router dump), `positive_gain` training reached holdout accuracy `0.647` / precision `0.600` / recall `0.750`, while `full_accept` reached `1.000` holdout on this small deterministic split. Treat this as a pipeline smoke and feature-sanity check, not a production router quality claim; the next gate needs a larger prompt suite and less policy-leaky features.
    - [x] Harden the offline router trainer against obvious leakage: add `--include-kind`, `--exclude-kind`, and `--no-sweep-feature` so training can drop fallback rows and remove `sweep_policy` as a categorical feature. On the same tiny dataset with `--exclude-kind target_only,neural_early_reject --no-sweep-feature`, `positive_gain` holdout dropped to `0.571` accuracy (`0.750` precision / `0.600` recall) and `full_accept` to `0.714` accuracy. This is the expected adversary signal: the previous high score was partly policy/row-mix leakage, and the next useful step is broader data, not runtime integration.
    - [x] Add `bin/qwen35_spec_router_prompt_suite.cr`, a synthetic non-private prompt-suite generator for router data collection. It emits `218` prompts across fact, code, repeat/template, structured, chat, math, and optimization-template categories, with `--format sweep` output directly accepted by `qwen35_speculative_sweep --only-prompts`. A fake-runner sweep smoke over the first five prompts verified label parsing and pipe-delimiter safety without model load.
    - [x] Run the first real suite shard: 27B+2B, first `12` suite prompts (`fact_*` plus early `code_*`), `tokens=4`, policies `default,ngram_router16_staged`, dumped to `/tmp/qwen35_router_suite_27b_2b_first12_t4`. The staged-router alias won `11/12` paired rows by wall (`avg delta -4.71ms/tok`, avg ratio `0.963x`) and improved average spec wall `79.21 -> 74.50ms/tok`, but both policies remained below plain target on average (`0.809x` and `0.830x`) because low-accept fact/code tails dominate. Extractor produced `43` cycle rows; adding them to the tiny training set worsened filtered `positive_gain` holdout to `0.545` accuracy, confirming we need much more balanced data before runtime use.
    - [x] Run the repeat-category contrast shard: 27B+2B, all `8` repeat prompts, `tokens=16`, policies `default,ngram_router16_staged`, dumped to `/tmp/qwen35_router_suite_27b_2b_repeat_t16`. Here `ngram_router16_staged` is a real speed path: average spec wall `53.43 -> 37.35ms/tok`, average speedup versus plain target `1.081x -> 1.959x`, paired wins `6/8`, avg ratio `0.683x`. Two structured-repeat cases (`yaml_hosts`, `json_pairs`) still regressed, so the useful router boundary is "strong repeated suffix with accepted n-gram", not repeat category alone. Extractor produced `75` rows; filtered `positive_gain` holdout over the combined small set stayed weak (`0.588` accuracy), so runtime integration remains blocked on broader data/features.
    - [x] Add `prompt_category` to router rows and train with it as a cheap pre-decode feature. Re-extracting the current dump dirs produced `203` rows; filtered training with category enabled (`--exclude-kind target_only,neural_early_reject --no-sweep-feature`) improved `positive_gain` holdout from `0.706` accuracy / `0.706` precision / `1.000` recall without category to `0.765` accuracy / `0.786` precision / `0.917` recall with category. This is still small-data evidence, but it confirms prompt class is useful and not just protocol decoration.
    - [x] Add `--holdout-by prompt` to the offline trainer so cycles from the same prompt cannot leak across train/holdout. On the current small combined set, prompt-group holdout is highly unstable (`holdout-every=3`: `0.792` accuracy; `4`: `0.571`; `5`: `0.000`), which is exactly the warning we needed: row-level metrics are not enough, and the next quality gate must report prompt-group splits over a larger suite.
  - [ ] Train and evaluate a tiny speculative router/acceptance predictor on a larger prompt suite, not a new draft model. The first smoke baseline exists; next quality gate should use `200-500` prompts and reduce policy leakage in the feature set. Candidate families: logistic regression / small MLP / gradient boosted tree over cheap features such as prompt class, last token ids/classes, n-gram match stats, previous cycle acceptance, current gamma, draft id, and draft top1/top2 margin if available. Hard budget: inference below `0.1-0.5 ms/token`; anything requiring a separate encoder pass or large ANN search is presumed to erase the speculative gain.
  - [ ] Add a default-off `--policy learned-router` / `QWEN35_SPEC_ROUTER_MODEL=PATH` experiment. The router chooses among target-only, n-gram, 0.8B draft, 2B draft, verifier mode, and gamma (`1/4/8/16/32`). It must fail closed to current exact policies when features/model are missing and must preserve greedy parity by target-verifying every proposed token.
  - [ ] Evaluate embedding/retrieval-style drafting only as prefix-continuation retrieval, not semantic next-word guessing. Candidate continuations must come from prior exact token histories or prompt-cache artifacts; approximate embedding similarity can choose which historical continuation to try, but target verification remains mandatory. Gate this behind the same router dataset because semantically similar continuations often fail exact token-id acceptance.
  - [ ] Run paired A/B for the learned/router policies against current defaults on 27B + 0.8B and 27B + 2B. Required evidence: per-prompt wall-time, acceptance distribution, fallback rate, draft/resync cost, and comparison to plain target. Promote only if it improves mixed prompt averages without regressing adversarial prompts beyond noise.
  - [x] Add `bin/qwen35_early_exit_probe.cr`, a default-off early-exit/self-speculation diagnostic. It runs the target model layer-by-layer, projects selected intermediate hidden states through the normal output norm + lm head, and compares each early `top1` against the full-depth greedy `top1` while reporting hidden cosine and relative delta. Initial 27B smokes over 32 generated tokens show layer-60 `top1` match only around `37.5%` on `def fibonacci(n):`, `The capital of France is`, and `Once upon a time`; layer-56 is around `25-31%`, and layers 40-52 are lower. Treat this as evidence against naive fixed-depth self-drafting.
  - [x] Extend the early-exit probe with a diagnostic confidence gate: full-logits `top1-top2` margin plus threshold precision/coverage tables. Layer-60 margin gating is still not strong enough (`margin>=4` gives only `66-83%` precision with `19-38%` coverage on the three smoke prompts). Layer-63 is predictably much closer (`81-97%` raw match; `margin>=2` gives `95-100%` precision with `62-97%` coverage), but exact self-speculation from layer 63 has little room to win because the draft has already paid for 63/64 target layers and still needs final target verification. Do not wire this into the speculative harness until a cheaper mid-layer gate or a shared-prefix verifier design changes that cost model.
  - [ ] If early-exit remains interesting, add JSONL dumps and train/evaluate a small gate over margin, token class, previous match streak, and prompt class. Promote only if the gate identifies high-precision exits no later than about layer 48-56, where skipped target work can plausibly outweigh extra head projections and verification.
  - [x] Fix `qwen35_prefill_attribution --compare-env=NAME=VALUE` parsing so benchmark scripts can pass the common `KEY=VALUE` form without crashing in `ENV[]=`; smoke `--prompt=8 --compare-env=QWEN35_Q4K_H16_GEMM_OFF=1` now reaches the paired A/B report.
  - [x] FFN-diamond lower-bound: a true no-activation-store prefill diamond is not a near-term route if it streams by `ffn_down` output tiles, because it would recompute the Q4 gate/up pair once per 64-row output tile. A layer-0 Q4/Q4/Q6 microbench measured current b64 `pair=1.861 ms`, `swiglu=0.172 ms`, `down=1.179 ms`, `chain=2.876 ms`; naive no-store recompute floor is `~120.47 ms` for the same layer (`~41.9x` slower). b256 showed the same shape (`9.508 ms` current vs `~392.52 ms` floor). Keep only algebraically new FFN diamonds alive; do not implement the naive tile-streamed version.
  - [ ] Next: target deeper batched speculative verification, lower-level Q4/Q6 tile-prepack/kernel changes, and DeltaNet associative block-scan research; Q8_0 draft is now closer, but exact speculative still trails plain target decode until verifier overhead is cut. FFN WBA remains only as a new-algorithm branch, not naive output-tile streaming.
  - [x] Save the RetNet-to-DeltaNet parallelization frame: the old CogniFusion RetNet trick works because `h_t = A*h_{t-1}+u_t` has cheap diagonal/scalar powers, while Qwen35 DeltaNet has `S_t = g_t*S_{t-1}*(I - beta_t*K_t*K_t^T) + beta_t*V_t*K_t^T`. This is still an associative affine transform over state, but naive dense block summaries would be too expensive; the viable research path is compact low-rank/WY-style block summaries plus per-block serial replay, not direct `conv1d/cumsum`.

## Deferred research backlog — efficient attention / long context

- [ ] **R.1** FlashAttention-style exact full-attention prefill kernel for Qwen35 full-attn layers; first add intra-group phase attribution and only implement if `full.attn` is material at pp2048+.
  - [x] Falsifier/current priority: pp2048 prefill attribution shows full-attention is not the wall yet (`attn wait 26.35 ms` inside `~10.17 s` profiled wall). The material wall is recurrent grouped prefill / DeltaNet serial work (`dn wait ~9991.69 ms`) plus large conversion traffic (`10.85 GiB`, 73% logical traffic), so FlashAttention is deferred until recurrent scan and conversion pressure are addressed.
- [ ] **R.2** DeepSeek Sparse Attention / NSA-style indexer experiment; requires calibration + sparse adaptation, not an exact drop-in for existing Qwen weights.
- [ ] **R.3** Linear-attention replacement track; treat as new architecture or distillation/continued-training project, not an inference-only optimization.
- [ ] **R.4** Training-free long-context inference hacks under eval gates: attention sinks / sliding window, SnapKV/H2O-like KV retention, and KV quantization. Keep all default-off until LongBench/RULER/top-logit drift evidence exists.
- [ ] **R.5** Exact DeltaNet associative block-scan prototype for long prefill.
  - [x] First prove equivalence on tiny CPU shapes (`s=8`) against the serial `delta_net_step!` recurrence: `spec/qwen35_deltanet_affine_scan_spec.cr` verifies both composed affine final state and block-prefix replay intermediate outputs (`2 examples, 0 failures`).
  - [x] Estimate work for `s=128` and prompt lengths `64/256/1024/2048` with a `1.25x` continuation gate: `bin/qwen35_deltanet_scan_model.cr` shows naive dense summaries fail, while compact summaries can pass pp256 if one summary composition stays below about `38-47` serial-token-equivalent DeltaNet steps for block sizes `8/16/32`.
  - [x] Add a dense CPU block-scan baseline (`bin/qwen35_deltanet_dense_scan_cpu.cr`) for exactness/overhead checks before Metal work. It reproduces serial final state on `s=16,tokens=64,block=16` and `s=32,tokens=128,block=32` with max state deltas below `1e-16`, but sequential CPU dense summary+replay is `~12-19x` slower than serial, confirming dense summaries are only plausible as a parallel long-prefill GPU baseline, not a short-prompt CPU-style algorithm.
  - [x] Extract reusable block-scan algebra into `ML::GGUF::Qwen35DeltaNetBlockScan`, so the proof spec and dense baseline share the same serial, affine, composition, replay, and compact-additive-summary formulas before any Metal prototype.
  - [x] Prototype compact block summaries using dense `A` plus low-rank `B = Σ left_i ⊗ right_i`, including compact apply and compact summary composition without materializing dense `B`. Focused specs pass (`8 examples, 0 failures`); sequential CPU compact is still slower than serial but faster than dense-summary CPU on checked shapes, and the storage model becomes favorable for Qwen shape `s=128` with block sizes below `64`.
  - [x] Prototype compact/WY-style transition summaries for `A`: `A = gamma * (I + U V^T)` now has exact build, apply, and composition checks. Focused specs pass (`11 examples, 0 failures`); the `--fully` CPU baseline avoids dense `A/B` materialization and is exact, but still sequentially slower than serial. This keeps the Metal path alive while confirming CPU-style execution is not the win.
  - [x] Build default-off Metal microbench for fully compact summary apply at synthetic Qwen shape (`s=128`, block `8/16/32/64`). The two-kernel `coeffs + apply` probe is exact within f32 drift (`max_delta < 3e-8`) and measures p50 `~0.257/0.269/0.293/0.322 ms` for block `8/16/32/64`; this excludes summary construction and prefill integration, so it is a green apply-kernel gate, not a full scan speedup claim.
  - [x] Add Metal compact-`A` construction microbench (`bin/qwen35_deltanet_compact_build_metal_micro.cr`). It is exact within f32 drift but only becomes faster than CPU construction at larger blocks: p50 `~0.223/0.281/0.442/1.108 ms` for block `8/16/32/64`, with CPU-build speedup `0.07x/0.24x/2.13x/1.24x`. This says block `32` is the first plausible Metal construction point; block `8/16` need fusion with apply/replay or they lose to dispatch/setup overhead.
  - [x] Add Metal compact-`B` right-factor construction microbench (`bin/qwen35_deltanet_compact_b_metal_micro.cr`). The direct per-rank vector kernel is exact within f32 drift (`max_right_delta < 5e-8`) and measures p50 `~1.206/1.086/2.034/2.835 ms` for block `8/16/32/64`; this makes compact-`B` construction the dominant current overhead and points to block `16` as the best early candidate before replay/scan costs.
  - [x] Falsifier: current compact block-scan lower-bound loses badly to `delta_net_chunk_128_rowwise` before replay/integration. The synthetic `build(A+B)+apply(final state)` path is exact within f32 drift, but at `s=128` block `8/16/32` p50 is `~1.300/2.324/3.566 ms` versus rowwise `~0.270/0.287/0.318 ms`. Since rowwise also produces per-token outputs and compact currently does not, do not integrate this compact-summary design into prefill without a new fused construction/replay algorithm.
  - [x] Apply the LTP/WBA frame to compact-`B` construction: a threadgroup-wide right-factor kernel parallelizes the `s=128` vector recurrence and remains exact within f32 drift (`max_right_tg_delta ~3.2e-8`). At block `16`, the microbench improved scalar compact-`B` p50 from `~2.18 ms` to `~0.30 ms` in a formatted rerun (`~7.2x`), with earlier short sweeps showing `~4.8-11.3x` over blocks `8/16/32`. This validates compact-`B` as an LTP/WBA target.
  - [x] Falsifier/update: replacing the separate compact `coeffs + apply` stages with a row-threadgroup apply kernel did not improve the full lower-bound; block `16` stayed around `~0.42-0.52 ms` and still failed to robustly beat rowwise. Keep the row-threadgroup apply route opt-in for research only; the next useful branch is multi-block parallel summary build + prefix/replay, not more single-block apply fusion.
  - [x] Add a multi-block summary-build gate to `bin/qwen35_deltanet_compact_b_metal_micro.cr`. At block `16`, compact `A+B` summary construction for all blocks in parallel is exact within f32 drift and becomes cheaper than rowwise for long prompts: blocks `16/64` measured summary-build p50 `~0.338/0.235 ms` vs rowwise chunk `~0.496/0.790 ms`. Blocks `1/4` still lose or remain marginal, so this is long-prefill-only evidence.
  - [x] Add rank-growth adversary modeling to `ML::GGUF::Qwen35DeltaNetScanModel`. Uncapped compact prefix composition is a dead end for long prompts (`pp1024` speedup `~0.28x`, `pp2048` `~0.14x`) because rank grows `16 -> ... -> tokens`. An optimistic exact rank cap at `s=128` makes `pp1024/pp2048` plausible (`~1.71x/~2.66x`) but still does not pass `1.25x` at `pp256`; the next prototype must include exact rank/basis compression or avoid rank-growing summary composition entirely.
  - [x] Prove exact CPU row-basis rank compression for fully compact summaries: `compress_fully_compact_row_basis` materializes the dense delta/additive terms only in the proof path, refactors them through row-basis factors, and caps transition/B rank at `<= s` without changing `A`, `B`, or applied state. Focused specs pass (`14 examples, 0 failures`), including compress-after-each-compose over 8 blocks.
  - [x] Falsifier: naive exact row-basis prefix composition is too expensive as a custom scalar Metal kernel. `bin/qwen35_deltanet_row_basis_compose_micro.cr` composes row-basis summaries exactly (`D_out=D1+D2+D1*D2`, `B_out=B2+gamma2*B1*(I+D2)`), but pp1024-like prefix levels sum to `~3.04 ms` and pp2048-like levels to `~4.02 ms`, while the synthetic rowwise baselines are only `~0.79 ms` and `~1.24 ms`. Keep row-basis compression as an algebra proof; do not build production scan on this naive dense-compose kernel.
  - [x] Prove an exact factor-basis compression alternative that avoids dense row-basis materialization: `compress_fully_compact_factor_basis` compresses compact outer factors by choosing an independent left-factor basis and accumulating transformed right factors. It preserves transition/B application, keeps ranks `<=s`, and keeps prefix composition rank-capped in focused randomized specs (`16 examples, 0 failures` with scan-model specs).
  - [x] Falsifier: direct GPU-serial factor-basis compression is not viable. `bin/qwen35_deltanet_factor_basis_compress_micro.cr` is exact within f32 drift after storing the reduced basis, but p50 is `~47.9/189.4/755.1 ms` for `32/64/128` factors at `s=128`, orders of magnitude above the rowwise baseline. This refutes serial basis selection/elimination as a Metal prefix path.
  - [x] Save the post-falsifier roadmap: the next exact summary-scan attempts are (1) adjoint/query-side block replay (`y_t = S_start * (A_prefix_t*q_t) + B_prefix_t*q_t`) to remove serial replay inside blocks, (2) a genuinely tiled/MMA `128x128` compose gate instead of scalar row-basis compose, and (3) fixed-basis residual-checked compression on real Qwen K vectors. Static lookup tables do not solve unseen prompts because `K/V/g/beta` are hidden-state dependent; prompt/session cache remains a separate exact product path.
  - [x] Prove adjoint/query-side block replay on CPU: `adjoint_replay_block` computes per-token transformed queries and additive output terms so `y_t = (S_start * transformed_q_t + additive_t) * scale` reproduces serial block outputs and final state exactly. Focused specs pass (`18 examples, 0 failures`), including block-prefix replay over multiple chunks. This is an algebra gate only; transformed-term construction still needs a Metal cost gate.
  - [x] Add an adjoint output replay lower-bound microbench (`bin/qwen35_deltanet_adjoint_replay_micro.cr`). With transformed queries/additive terms precomputed, Metal output replay is exact within f32 drift (`max_output_delta ~8.5e-8`) and beats synthetic rowwise chunk replay in the checked shape: stable p50 `~0.232/0.213 ms` vs rowwise `~0.316/0.301 ms` for tokens `16/32`. This keeps adjoint replay alive, but term construction is still excluded.
  - [x] Falsifier: the full adjoint transformed-term cost gate is exact but not a speed win against the current rowwise chunk. The reverse threadgroup kernel constructs `transformed_q/additive` within f32 drift (`max_tq_delta <= 2.91e-8`, `max_add_delta <= 9.66e-8`), but `terms+output` and fused `terms->output` lose once term construction is included. A fairer rerun with precomputed rowwise reset data measured fused-vs-rowwise p50 ratios `0.81/0.79/0.85/0.82/0.78/0.72x` for tokens `4/8/16/32/64/128`. Keep adjoint replay as an algebraic tool, not the next production prefill path.
  - [x] Re-check fixed-basis low-rank DeltaNet on real 27B `k` vectors with `bin/qwen35_deltanet_fixed_basis_probe.cr`. On a varied 80-token prompt, per-layer rank-64 PCA does not make residuals small (`p50` residual roughly `0.49/0.34/0.42` for layers `0/8/16`), but full-model drift is surprisingly tolerant: approximating one layer at a time gives `97.5-100%` teacher-forced top1 and `100%` 12-token greedy top1; approximating layers `0,8,16` together gives `97.5%` teacher-forced top1 and `93.75%` 16-token greedy top1. This is not an exact scan win, but it opens a separate default-off approximate/guarded recurrent-layer compression branch with target-verifier gates.
  - [x] Follow-up low-rank DeltaNet sweep: rerunning with enough calibration vectors for true rank-64 (`tokens=128`, `calib=64`) improves held-out K residuals versus rank-32 (`p50 0.328` vs `0.475`, `p90 0.511` vs `0.698`) and preserves short greedy parity on a mixed code/markdown prompt. Approximating sparse recurrent layers `0,8,16` gives `98.44%` teacher-forced top1 and `100%` 12-token greedy top1 for both rank-32/64; approximating early recurrent layers `0,1,2,4,5,6` gives `100%` teacher-forced and `100%` 12-token greedy top1, with rank-64 reducing KL/max-delta (`mean_kl 0.0036` vs `0.0121`). A smaller `calib=32` sweep across code/facts also kept 12-token greedy parity for early layers, while mid layers `8,9,10,12,13,14` showed a greedy mismatch. Prioritize early-layer approximate compression first.
  - [ ] Add a paired approximate low-rank DeltaNet eval harness before any Metal kernel work: sweep layers/ranks/prompts, measure top1 parity, KL, generated drift, and speculative verifier acceptance. Only if rank `32-64` over many recurrent layers preserves greedy/speculative acceptance should we build the reduced-state Metal kernel (`S` as `[state_size x rank]` instead of `[state_size x state_size]`).
  - [x] Falsifier/narrowing: tiled row-basis `128x128` prefix compose improves the old scalar compose gate by `~1.3-1.8x` and stays exact within f32 drift, but it still does not close the long-prefill scan budget. Summing prefix levels gives roughly `1.88 ms` for pp1024-like `64` blocks and `2.44 ms` for pp2048-like `128` blocks, still above the old synthetic rowwise baselines (`~0.79/1.24 ms`) before replay/integration costs. Dense row-basis compose needs a bigger algorithmic change than ordinary threadgroup tiling.
  - [x] Falsifier/narrowing: fixed per-head low-rank bases for real normalized DeltaNet `K` vectors are not an obvious exact compression lever. A real-Qwen probe using greedy modified Gram-Schmidt on calibration tokens shows held-out residuals remain large: layer0 with 64-token calibration has rank64 mean/p50 residual `0.322/0.332`, p90 `0.546`; layer1 with 32-token calibration has rank32 mean/p50 residual `0.533/0.532`, p90 `0.711`. Follow-up pass-rate reporting shows the nuance: layer0 rank64 has `53.12%` of held-out head/token vectors below residual `0.35` and `82.62%` below `0.5`, but layer1 rank32 has only `7.23%` below `0.35` and `41.41%` below `0.5`. This may be useful only as approximation+fallback with eval gates, not as the current exact rank cap.
  - [x] Re-open the fixed-basis idea as an approximate projected-DeltaNet branch: PCA bases improve residuals modestly, but the important finding is that projected-K DeltaNet output/state drift can stay tiny even when K residuals are large. With PCA rank `16/24`, layers `1/2/4` on varied prompts show `y_rmse ~8.5e-5..1.26e-4`, `state_rmse ~0.0014..0.0017`, while K residual p50 remains `~0.49..0.65`. Layer0 remains risky (`y_rmse ~0.004-0.006`, state max around `9`), so this needs per-layer gating/eval. If `K≈Bc`, the DeltaNet state can be stored as `M B^T` with shape `128 x rank` and outputs use `M*(B^T q)`, giving a plausible approximate speed path independent of exact associative scan.
  - [x] Prove the projected-K low-rank DeltaNet state formula on CPU: for fixed per-head basis `B` and `K_proj = Bc`, the full projected recurrence is exactly represented by `S = M B^T`, update `M = gM + beta*(V - gM c)c^T`, and output `Y = M*(B^T Q)`. The probe verifies low-rank `M` against full projected `128x128` recurrence with proof drift around `1e-8` for outputs and `<=1.15e-5` for reconstructed state. Approx drift versus exact remains layer-dependent: layer1/2/4 rank16/24 stay around `lr_exact_y_rmse ~8.5e-5..1.82e-4` on two prompts, while layer0 remains riskier (`~0.0028..0.0052`).
  - [x] Add a full-model logit drift gate for projected-K low-rank DeltaNet: one target recurrent layer switches to low-rank `M` state after a calibration prefix, all remaining layers run normally, and logits/top1 are compared to exact. On a varied technical prompt with `tokens=48/calib=24/rank=24`, layers `0/1/2/4` all kept `top1_match=100%` and mean cosine `0.99937..0.99992`. Adversary mixed code prompt is weaker: layers `0/2/4` keep `95.83%` top1 and mean cosine `0.9974..0.9991`, while layer1 rank24 drops to `91.67%` top1 / `0.9940` mean cosine; rank32 with longer calibration only improves layer1 to `93.75%`. This is promising but not robust enough for default-on approximation.
  - [x] Add cumulative multi-layer projected-K logit policy gates. With a corrected same-reference comparison, `layers=0,2` and `0,2,4` at `rank24/calib24` keep `100%` top1 on the varied prompt; the adversary prompt drops to `95.83%` without fallback. Residual-gated fallback at threshold `0.8` restores adversary `top1_match=100%` with mean cosine `0.99972` for `0,2` at `29.17%` approximate steps and `0.99881` for `0,2,4` at `36.11%` approximate steps. This is a real approximate/fallback opening, but still default-off and eval-gated.
  - [x] Add top-k/KL/logit-margin gates and run a four-prompt policy smoke (`varied`, `adversary_code`, `math_symbolic`, `dialogue_json`). Aggressive `layers=0,2,4/th=0.8` keeps `top5_hit=100%` and `confident_mismatches=0`, but top1 drops to `91.67%` on math/dialogue with tiny exact margins. The best strict-top1 smoke so far is `layers=0,4/th=0.5`: all four prompts keep `top1/top5=100%`, `max_kl <= 0.00305`, and `confident_mismatches=0`, but approximate-rate is only `0..14.58%`.
  - [x] Add a teacher-forced exact-greedy generation drift gate (`--simulate-generate=N`). This is a stronger falsifier than prompt-token logits: prompt-safe `layers=0,4/th=0.5` still flips one low-margin greedy token on math/dialogue, and aggressive `0,2,4/th=0.8` drops to `75..93.75%` greedy top1 over 16 decode tokens. A stricter `0,4/th=0.35` restores generation top1 on the four-prompt smoke, but approximate-rate is only `0..5%`, too low for a meaningful speed path.
  - [x] Re-test projected-K generation drift with true rank-64 and multi-threshold residual fallback in one process (`--simulate-fallback-thresholds`). On the four-prompt smoke (`varied`, `adversary_code`, `math_symbolic`, `dialogue_json`), `layers=0,2,4/rank64/th=0.8` keeps `100%` 16-token teacher-forced greedy top1, `top5_hit=100%`, and `confident_mismatches=0`, while reaching useful approximate rates: `97.22%`, `65.28%`, `84.72%`, and `95.83%`. Lower thresholds also stay exact but trade coverage down. This overturns the earlier rank24 warning for the same layer set; the branch now needs a larger prompt-suite gate before any Metal work.
  - [x] Expanded projected-K low-rank policy smoke on 27B: `layers=0,2,4/rank64/th=0.8` kept `100%` 24-token teacher-forced greedy top1 on six prompt classes (`varied`, `adversary_code`, `math_symbolic`, `dialogue_json`, `repetition_yaml`, `logs_sql`) with useful approximate rates (`70.83-97.02%`, lowest on code adversary). A stronger `gen=64` stress on `adversary_code` and `logs_sql` also kept `100%` greedy top1 / `top5_hit=100%` / `confident_mismatches=0`, with approximate rates `75.35%` and `97.57%`. This is still smoke-level (`G` limited by prompt count), but it reopens the branch as a serious speed candidate.
  - [x] First expanded falsifier for `layers=0,2,4/rank64/th=0.8`: a 12-prompt `gen=32` suite kept `100%` greedy top1 on 11/12 prompts, but a math/facts prompt flipped one low-margin formatting token (`" then"` -> `"\n\n"`) while keeping `top5_hit=100%` and `confident_mismatches=0`. Tightening K-residual fallback down to `0.35` and approximate-output margin fallback did not fix it. The current fallback is not a true exact resync; it exact-steps from reconstructed low-rank state, so accumulated state drift can survive local residual gates. An oracle shadow-state refresh every 4 approximate-eligible positions fixed the prompt, while 8/16 did not.
  - [x] Add a self-spec low-rank simulation mode (`--simulate-self-spec-gammas`) that resyncs a projected-K low-rank draft branch from the exact checkpoint at each chunk, lets it free-run for `gamma`, and exact-verifies the proposed chunk. This converts low-rank drift into rejections instead of wrong output. On 27B `layers=0,2,4/rank64/th=0.8/gen=32`, five prompt smoke (`varied`, `code_fib`, `logs_sql`, `json_only`, plus the previous `math_paris` falsifier) emitted exact token ids. `varied/code/logs/json` had `100%` accept at `gamma=4/8`; `varied` also had `100%` accept at `gamma=16/32`. The math falsifier had one rejection: `gamma=4/6/8` accepted `31/33` proposals (`93.94%`), while `gamma=16/32` accepted `31/41` (`75.61%`) but still emitted exact ids. This is the strongest evidence so far that projected-K should be treated as an internal draft branch, not target replacement.
  - [x] Add a relative self-spec cost model to the probe. With conservative hypothetical costs (`draft=0.20`, `exact verifier token=0.65`, `chunk overhead=0.03`, `correction=1.0`, normalized to plain exact decode token = `1.0`), `varied` estimates `~1.17x` at `gamma=4/8/16`, while the math falsifier estimates `~1.09x` at `gamma=4/8` but drops to `0.89x` at `gamma=16` because one rejection wastes a longer proposal. This makes adaptive gamma a first-class policy requirement: long chunks are valuable only after recent full accepts or high confidence.
  - [x] Implement adaptive low-rank self-spec gamma in the probe (`--simulate-self-spec-adaptive=MIN,START,MAX`) plus optional margin-gated growth (`--simulate-self-spec-adaptive-grow-margin`). First smoke shows naive growth is too aggressive for the math falsifier: `4->8->16` keeps exact output but estimates only `0.98x` under the conservative cost model, worse than fixed `gamma=4/8` (`~1.09x`); capping at `8` or requiring previous-chunk margin `>=0.5` still does not help because the risk appears inside the future chunk, not in the previous verifier chunk. Good prompt `varied` still estimates `~1.17x`.
  - [x] Add self-spec draft-risk diagnostics: per-chunk `draft_min_margin`, low-margin counts, optional draft stop-margin, and draft top-k hit rates. The math falsifier rejects despite high draft margins (`draft_min_margin` per chunk `5.09/2.18/2.90`, low-margin count `0` at threshold `0.5`), so draft margin is not a useful risk gate there. However, the rejected exact token was inside draft `top5` but not `top2` (`reject_top5_hits=1`, `reject_top2_hits=0`), which makes top-k/tree verification a more promising next branch than margin gating.
  - [x] Add a cheap top-k rescue simulation (`--simulate-self-spec-topk-rescue=K`): if a greedy reject's exact token is in the draft top-K, model it as a tree-rescued token instead of a correction. On the math falsifier with `gamma=16/top5`, the rejection is rescued (`topk_rescues=1`, `correction_steps=0`), but the conservative estimated speed only improves from `0.89x` to `0.916x` because the long wrong-path proposal was already generated and verified. Leaf rescue alone is insufficient; the next tree experiment must avoid paying for the wrong tail.
  - [x] Add progressive self-spec schedule simulation (`--simulate-self-spec-progressive=4,4,8`): instead of one long `gamma=16` proposal, generate/verify shorter chunks and reset the schedule on rejection. Under the conservative cost model, `varied` stays near fixed `gamma=16` (`1.169x` vs `1.174x`), while the math falsifier recovers the fixed `gamma=4/8` economics (`1.092x` vs `0.890x` for fixed `gamma=16`) by reducing proposed/verifier tokens from `41` to `33`. This is the first dynamic policy that keeps the long-chunk upside while avoiding the known wrong-tail loss.
  - [x] Add an overlap cost model for pipelined draft/verifier (`--self-spec-overlap-cost`): per segment cost is `max(draft_segment_cost, verifier_segment_cost) + overhead` instead of the sum. This is an optimistic upper bound for running the low-rank draft lane while the exact verifier lane checks the current segment. With the same conservative unit costs, progressive `4,4,8` estimates jump from `~1.17x/1.09x` (sum model) to `1.53x` on `varied` and `1.41x` on `math_paris`. This makes a two-lane prototype a serious next engineering branch, provided state resync/command-buffer overhead does not erase the overlap.
  - [x] Add partial-overlap efficiency to the cost model (`--self-spec-overlap-efficiency=F`): the overlapped segment cost is now `draft + verifier - efficiency * min(draft, verifier) + overhead`, where `1.0` matches the previous perfect-overlap model and `0.0` falls back to the sum model. A smoke with `efficiency=0.5`, progressive `4,4,8`, and the same unit costs still estimates `1.323x` on `varied` and `1.231x` on `math_paris`, so the two-lane branch does not require perfect fusion to remain interesting.
  - [x] First two-lane implementation slice: split `Qwen35Metal.forward_decode_wave` into a synchronous wrapper plus `forward_decode_wave_async`, returning a `DecodeWaveSubmission` that owns command buffers and output buffers until `wait_forward_decode_wave`. Add a fresh-scratch lease path (`fresh_scratch: true`) so future in-flight draft/verifier submissions can avoid pooled scratch buffer races. Thread this through `Qwen35CPU.forward_decode_wave_routed_async` so a controller can submit from normal token/state inputs without duplicating Metal KV/SSM buffer preparation. This does not yet overlap real low-rank draft and exact verifier work; it creates the required no-wait/readback seam.
  - [x] Add the first exact/exact overlap probe (`bin/qwen35_two_lane_overlap_probe.cr`) and lane-local scratch namespaces. A pure fresh-scratch async path was safe but mostly paid allocation overhead; lane namespaces (`probe_a`, `probe_b`) preserve scratch reuse while avoiding in-flight buffer races. On Qwen3.5-9B `The capital of France is`, queued two-wave submission measured `1.13x` at `pairs=4` (`167.9ms` pooled sequential -> `148.3ms`) and `1.11x` at `pairs=8` (`331.7ms` -> `298.9ms`). This is not proof of GPU compute overlap, but it shows command-buffer/CPU encode/barrier work can be hidden before the low-rank draft kernel exists.
  - [x] Add a wall-clock low-rank self-spec probe (`--simulate-self-spec-wall-progressive=4,4,8`) that measures projected-K draft proposal time separately from the exact chunk verifier path. On Qwen3.6-27B/rank64/layers `0,2,4`/`gen=16`, exactness held with `100%` acceptance, but the CPU low-rank draft branch dominated (`draft_ms=2853`, `verifier_ms=732`, `overlap_est=1.26x`). This validates the two-lane semantics but makes the next implementation gate explicit: port the low-rank recurrent draft step to Metal/lane-local scratch; otherwise overlap only hides the smaller exact verifier cost.
  - [x] First Metal low-rank core kernel: add `lowrank_delta_step` to `delta_net.metal` plus `Qwen35Metal.lowrank_delta_step` and `--simulate-lowrank-metal` CPU-vs-Metal proof. Accuracy is good (`9B` rank64 `y_max=2.4e-7`, `state_max=3.8e-6`; `27B` rank64 `y_max=4.8e-7`, `state_max=5.1e-6`). Timing shows the single-step wrapper is dispatch/readback bound (`9B`: `6.77ms` Metal vs `8.79ms` CPU over 24 steps; `27B`: `18.07ms` Metal vs `14.27ms` CPU), so the next gate is a GPU-resident chunk/lane stage that computes `c/qbar`, updates multiple tokens, and avoids per-step readback.
  - [x] Integrate the Metal low-rank core into the wall-clock self-spec draft path (`--simulate-self-spec-wall-metal-lowrank`). Exact/emitted ids stay identical. On 9B/gen8, CPU-core draft and Metal-core draft are effectively tied (`400.8ms` -> `398.6ms` draft time); on 27B/gen8, Metal-core wall run holds exactness with `draft_ms=952.9`, `verifier_ms=358.8`, `overlap_est=1.38x`. This confirms the state kernel can sit inside the draft lane, but the draft branch is now dominated by CPU projections/FFN/logit/head and per-step wrapper overhead. Next: move coefficient projection (`K/Q -> c/qbar`) and multi-step low-rank recurrence into one GPU-resident chunk command buffer before optimizing the row kernel.
  - [x] Integrate GPU coefficient projection into the low-rank draft core (`lowrank_project_coeffs` + `Qwen35Metal.lowrank_delta_step_projected_buf`, gated by `--simulate-self-spec-wall-metal-project`). Basis buffers are cached in `LowRankState` to avoid per-step basis upload. Exact/emitted ids stayed identical. 9B/gen8 measured `draft_ms=358.0` with `metal_project=1` (no regression vs CPU-coeff Metal core), and 27B/gen8 measured `draft_ms=921.9`, `verifier_ms=358.7`, `overlap_est=1.39x`. The remaining bottleneck is now the per-token wrapper/readback and the non-low-rank layer work; next step is chunk-resident proposal generation across `gamma` tokens in one lane command buffer.
  - [x] Add the first chunk-resident low-rank recurrent core: `lowrank_project_coeffs_chunk` + rank-parallel `lowrank_delta_chunk_step_parallel`, wrapped by `Qwen35Metal.lowrank_delta_chunk_projected_buf` and tested with `--simulate-lowrank-metal-chunk`. After fixing projection dispatch to use real threads instead of one-thread threadgroups, one command buffer for 24 steps beats CPU proof (`9B`: `9.63ms -> 4.55ms`, `y_max=6.6e-7`; `27B`: `13.99ms -> 4.04ms`, `y_max=1.19e-6`). This validates the chunk-resident recurrent core; the next integration is feeding this from a draft lane chunk without per-token logits/readbacks, or using it for verifier-known spans/tree branches where token ids are already known.
  - [x] Fuse the low-rank chunk core through recurrent post-processing and `ssm_out`: `Qwen35Metal.lowrank_delta_chunk_projected_out_buf` now runs `K/Q -> c/qbar`, low-rank `M` scan, `delta_net_post_norm_gate_chunk`, and batched `ssm_out` in one command buffer, tested by `--simulate-lowrank-metal-chunk-out`. CPU-vs-Metal proof stays close (`9B` `out_max=0.0011`, `27B` `out_max=0.0033`) and removes the `y` readback. Warm 24-token timings improve vs CPU proof (`9B`: `17.6ms -> 11.6ms`; `27B`: `35.5ms -> 13-18ms`, noisy), so the next gate is wiring this into the draft proposal lane and measuring top1/head residency instead of stopping at `attn_out`.
  - [x] Add a full recurrent-layer chunk bridge for known spans (`--simulate-lowrank-metal-layer-chunk`): samples now retain the layer input row, Metal computes the fused low-rank attention branch through `ssm_out`, and the probe finishes residual+FFN on CPU to compare final layer rows against per-token CPU low-rank. Accuracy stays close (`9B` `layer_max=0.0010`, `27B` `layer_max=0.0029`) and sequential 24-token timings still improve despite the CPU FFN tail (`9B`: `45.4ms -> 29.0ms`; `27B`: `79.6ms -> 58.6ms`). The next useful fusion is post-attn residual/RMSNorm + FFN/head residency; otherwise FFN/head CPU work dilutes the low-rank chunk gain before the two-lane scheduler can exploit it.
  - [x] Fuse the known-span low-rank recurrent layer fully on Metal (`Qwen35Metal.lowrank_delta_chunk_projected_layer_buf`, `--simulate-lowrank-metal-layer-full`): one command buffer now runs low-rank attention through `ssm_out`, residual add + post RMSNorm, FFN gate/up, SwiGLU, FFN down, and final add. CPU-vs-Metal proof remains close (`9B` `layer_max=0.00092`, `27B` `layer_max=0.00253`) and the full 24-token layer timing improves strongly (`9B`: `48.9ms -> 16.1ms`; `27B`: `77.1ms -> 29.1ms`). This is now a credible full-layer known-span primitive for tree/verifier-known spans and for a future layer-major draft replay stage.
  - [x] Add async/no-wait submission for the full low-rank recurrent layer chunk (`Qwen35Metal.lowrank_delta_chunk_projected_layer_async`, `LowRankLayerChunkSubmission`, `wait_lowrank_layer_chunk`) plus `--simulate-lowrank-metal-layer-overlap`. The sync wrapper still behaves as before, while two independent 24-token low-rank layer chunks can now be queued with separate scratch namespaces and waited later. Smoke output matched exactly (`output_max=0`), and the independent-submission microprobe originally reported substantial hidden time (`9B`: `27.7ms serial -> 8.5ms queued`, `3.26x`; `27B`: `28.8ms -> 9.4ms`, `3.08x`). TAINT/REFUTED 2026-04-27: later review/reruns did not reproduce the 3x timing (`~1.07-1.20x` instead), and this is an independent known-span layer-chunk proxy, not final autoregressive draft throughput or the requested `self-draft(N+1) + validator(N)` scheduler.
  - [x] Add the first draft/verifier overlap proxy (`--simulate-lowrank-metal-verifier-overlap`): submit one async low-rank full-layer chunk, run exact `prefill_tokens_top1s` on the held-out candidate span, then wait the draft lane. Correctness held (`draft_output_max=0`, verifier top1s matched), but measured overlap was essentially zero (`9B`: draft `2.94ms`, verifier `143.5ms`, serial `146.5ms`, overlap `146.5ms`; `27B`: draft `4.69ms`, verifier `352.0ms`, serial `356.7ms`, overlap `356.6ms`). This falsifies the idea that async draft alone is enough: the current exact verifier path is synchronous/internal-wait heavy and/or serialized on the same Metal queue. Next gate is an async exact verifier chunk/top1 path, not more draft-layer fusion.
  - [x] Add lane-local Metal command queues and a queued exact decode-wave verifier proxy (`--simulate-lowrank-metal-decode-verifier-overlap`). Known candidate tokens can be submitted through `forward_top1_async` without waiting between tokens because state dependencies live in ordered Metal buffers. Correctness held (`lr_decode_verify_match=true`, `draft_output_max=0`). Queued exact decode improved over serial decode (`9B/8 tokens`: `167.3ms -> 143.2ms`, `1.17x`; `27B/8 tokens`: `467.1ms -> 439.9ms`, `1.06x`), but overlap with a single low-rank layer chunk remained negligible (`hidden_ms~0.4-0.5ms`, `overlap_speedup~1.00x`). This means the verifier async primitive works, but a one-layer draft segment is too small relative to the verifier; next proxy should queue a layer-set/full-draft replay or tree batch, not just layer0.
  - [x] Add WBA timeline tracing for the decode-verifier overlap proxy (`QWEN35_WBA=1`). The timeline shows the real bottleneck is verifier submission/encoding, not draft math: on 9B/8 tokens, verifier submit grows from `~2-4ms` to `~17-19ms` per later token and waits consume `~60ms`; draft submit is `~0.5ms` and draft wait is almost fully hidden (`0.024ms`). On 27B/8 tokens, verifier submit is `~52-68ms` for later tokens, verifier wait is `~94ms`, and draft wait can become visible (`16.5ms`) due resource contention. LTP should therefore target verifier batching/layer-group submission and fewer per-token decode-wave encodes before trying more queues or more draft fusion.
  - [x] Add exact verifier LTP proxy (`--simulate-exact-verifier-ltp`) comparing serial decode waves, queued decode waves, and chunk-major `prefill_tokens_top1s` on the same held-out candidate span. Top1 ids match for queued and chunk-major routes. For 8-token spans, chunk-major verifier beats serial decode by `1.52x` on 9B (`168.6ms -> 110.7ms`) and `1.45x` on 27B (`469.8ms -> 323.4ms`), while queued decode gives only `1.07-1.15x`. This confirms the next production step: async/no-wait chunk-major verifier, not more per-token decode-wave scheduling.
  - [x] 2026-04-27 kernel gate before resuming self-draft/validator parallelization: inventory found one unused added Metal entrypoint (`lowrank_delta_chunk_step`) with no Crystal wrapper or probe coverage, so it was removed. Current added acceleration kernels are `lowrank_project_coeffs`, `lowrank_project_coeffs_chunk`, `lowrank_delta_step`, and `lowrank_delta_chunk_step_parallel`. Rebuilt `/tmp/qwen35_probe_kernel_gate` and reran 9B layer2 ranks `32,48,64` with `--simulate-lowrank-metal`, `--simulate-lowrank-metal-chunk`, `--simulate-lowrank-metal-chunk-out`, `--simulate-lowrank-metal-layer-chunk`, and `--simulate-lowrank-metal-layer-full`: all CPU-vs-Metal checks stayed within tiny error (`lr_chunk_y_max=1e-8`, `lr_chunk_state_max=1.2e-7`, `lr_chunk_out_max=0.00022525`, `lr_layer_full_max=0.00108004`). Focused specs `spec/qwen35_delta_net_spec.cr spec/qwen35_forward_spec.cr` passed (`17 examples, 0 failures`). 27B was not rerun because no local `*27B*Q4_K_M*.gguf` was found under `~/.cache/lm-studio/models`.
  - [x] 2026-04-27 hostile component speed gate with the real 27B path (`/Users/sergey/.cache/lm-studio/models/lmstudio-community/Qwen3.6-27B-GGUF/Qwen3.6-27B-Q4_K_M.gguf`): added `--simulate-lowrank-metal-project` to isolate single-token Metal Q/K projection plus low-rank step. On 9B/27B layer2 rank64, `lowrank_delta_step` and `lowrank_project_coeffs + lowrank_delta_step` are accurate (`y_max<=1e-8`, state max `<=1.2e-7`) and faster than the CPU proof, but Metal projection adds little over CPU-coeff Metal step at single-token granularity (`9B 21.19ms -> 22.33ms`, `27B 24.08ms -> 25.27ms` for 32 steps). The real local accelerator is chunk residency: `lowrank_project_coeffs_chunk + lowrank_delta_chunk_step_parallel` runs 32 steps in `4.24ms` on 9B and `4.03ms` on 27B vs CPU proof `536.5ms`/`811.1ms`; chunk-out and full-layer known-span paths are also accurate and much faster than CPU proof (`27B full-layer 64-step reruns ~16-18ms Metal vs ~1040-1063ms CPU proof`, `layer_max=0.00179482`). However, current autoregressive self-draft is not yet faster than exact (`9B 188.9ms draft vs 180.1ms exact`, `27B 630.5ms draft vs 493.9ms exact`, agreement `8/8`), and wall self-spec with `layers=0,2,4` remains draft-bound despite 100% acceptance (`9B metal_project draft/verifier 2489.7/144.1ms`, speedup_est `1.0579x`; `27B 4298.3/360.5ms`, `1.0839x`). Fresh verifier LTP is strong (`32-token`: `4.34x` 9B, `5.28x` 27B), but cross-queue draft/verifier overlap remains weak (`N=48`: `1.0628x` 9B, `1.0547x` 27B). Conclusion: the kernels accelerate the known-span pieces; the missing speedup is still controller/dataflow residency, not low-rank math correctness.
  - [x] 2026-04-27 cheap-draft falsifier sweep: added probe-only `--simulate-cheap-self-draft-variants` with `lowrank-no-ffn`, `skip-layer`, and `early-exit-N`. This confirms why the prefill/LTP idea helps the verifier but not proposal generation by itself: known candidate spans can run chunk-major, while unknown draft tokens still require autoregressive top1 decisions. On 9B `layers=0,2,4`, `lowrank-no-ffn` kept `100%` acceptance for `gen=4` but only reduced draft from `~1296ms` to `~1257ms`; `skip-layer` fell to `40%` acceptance and got slower. On 27B `layers=0,2,4`, `lowrank-no-ffn` fell to `50%` acceptance and `skip-layer` to `0%`. Narrower no-FFN layer sets did not generalize: 27B `layers=0` and `0,2` still fell to `50%` acceptance. Naive early-exit is worse: 9B `early-exit-8/16` had `0%` acceptance and `early-exit-24` `33%`; 27B short `gen=2` had `0%` acceptance for `early-exit-8/16/24`. Verdict: cheap proposal needs either a trained/calibrated draft head or a different draft model/state objective; naive layer skipping/early-exit is not a safe production branch.
  - [x] 2026-04-27 progressive tree oracle probe: added `--simulate-self-spec-tree-k=K`, which models low-rank top-K candidates chosen before generating a wrong tail. It reports exact parity, top1/topK hit rates, rank-ordered branch checks, full top-K branch checks, and the same unit cost model used by self-spec. Easy rank64 9B/27B smokes were all top1 (`100%`, no branching), so they do not justify tree work by themselves. A deliberately hostile 27B rank8/code-SQL prompt produced the needed signal: ordinary long self-spec with `schedule=16/top5-rescue` had one rejection after paying for 20 verifier tokens (`75%` accept, estimated `0.9379x`), while the tree oracle caught the same miss in top5 before the tail (`top1=93.75%`, `top5=100%`, `branch_tokens_rank=17`, `branch_tokens_full=20`, `rank_speedup=1.1204x`, `full_speedup=0.9858x`). Verdict: tree-before-tail is mathematically real but only attractive if the verifier can avoid full topK cost or can batch it cheaply; next gate is a prompt suite at production ranks (`32/48/64`) and a real batched verifier tree cost.
  - [x] 2026-04-27 tiny oracle-head falsifier: added `--simulate-topk-oracle=K` and `--simulate-topk-oracle-train-tokens=N`. This is not a full LM-head; it trains only token/rank bias and a margin gate over low-rank draft topK, then tests on later exact-greedy steps. Result: simple bias/rank reranking did not improve the tested cases. On 9B rank64 it correctly chose no-op because top1 was already `100%`. On 27B rank8 hostile code/SQL (`gen=32/top5`), the miss is inside top5, but if train does not include a miss the margin gate stays off and test keeps `baseline_top1=95.83%`, `baseline_avg_branch=1.042`; if train includes the miss, the learned threshold (`0.4007`) does not overgate clean test and still no rerank gain. Rank16/32 on the same prompt were also test-clean (`100%` top1). Verdict: a tiny token/rank bias is too weak and data-sparse; a useful oracle-head likely needs a real prompt suite plus richer features (draft margin history, low-rank residuals, layer-set/rank signals), not a full vocab LM-head.
  - [x] 2026-04-27 FFN sparsity quality gate: added probe-only draft variants `lowrank-ffn-top-P`, keeping only the top-P percent by absolute `SwiGLU = silu(gate) * up` activation before `ffn_down` inside low-rank recurrent draft layers. This currently measures quality/acceptance only, not real speed, because `ffn_down` still runs dense and top-M sorting adds overhead. Separate `layers=0,2,4/rank64` smokes kept `100%` acceptance for `top40/top20/top10` on 9B (`gen=8`) and 27B (`gen=4`). Hybrid early recurrent layers `0,1,2,4,5,6/rank64` also kept `100%` acceptance for `top40/top20/top10` on 9B (`gen=4`) and `top40/top20` on 27B (`gen=2`). A 9B all-recurrent short smoke (`0,1,2,...,30` excluding full-attn cadence, `gen=2`) kept `100%` acceptance for `top40/top20`. Verdict: FFN activation sparsity is the most promising next math lever, but the next gate must implement or simulate sparse `ffn_down` cost and run a larger prompt suite; do not treat the current dense-zero wall timings as speed evidence.
  - [x] 2026-04-27 FFN activation PCA quality gate: added `lowrank-ffn-pca-R`, which collects calibration `SwiGLU = silu(gate) * up` activations per selected recurrent layer, builds a PCA basis, and projects draft FFN activations before `ffn_down`. With `tokens=64/calib=32/pca_iters=4`, `layers=0,2,4/rank64` kept `100%` acceptance for PCA ranks `16/32/64` on 9B (`gen=4`) and `16/32` on 27B (`gen=2`), matching top-M quality in these tiny smokes. Hybrid early recurrent layers `0,1,2,4,5,6/rank64` kept `100%` acceptance for PCA `16/32` on 9B (`gen=2`) and 27B (`gen=1`). Caveat: the baseline low-rank drift on early6 already shows teacher-forced top1 misses on the held-out prompt (`93.75%` 9B, `96.88%` 27B), so this is only a small generation acceptance signal. Verdict: activation PCA is at least as interesting as top-M and much more compact (`rank16/32` vs thousands of FFN channels), but next gate must add a real cost model/kernel for `B^T a` + `WdB`, plus a larger prompt suite.
- [x] 2026-04-27 FFN PCA-down factorization gate: added probe-only `lowrank-ffn-pca-down-R`, which precomputes `WdB = ffn_down * basis` per recurrent layer and replaces dense `ffn_down(PCA(a))` in the low-rank draft with `sum_j (B_j^T a) * WdB_j`. This checks the actual `ffn_down` cheapening formula instead of merely zeroing/projecting activations before a dense matmul. 9B `layers=0,2,4/rank8/tokens=64/calib=32/gen=4` matched dense PCA quality (`lowrank-ffn-pca-32` and `lowrank-ffn-pca-down-32` both `100%` acceptance, parity true, same exact/emitted ids). Initial 27B stress with `rank8/calib16/pca16/gen2` showed PCA-down adds no extra degradation relative to dense PCA, but PCA itself reduced acceptance versus baseline. Fairer 27B reruns with projected-DeltaNet `rank64` recovered clean short-smoke quality: default prompt `calib32/pca-down32/gen4`, default prompt `calib64/pca-down64/gen4`, and adversarial code/SQL prompt `calib32/pca-down32/gen4` all kept `100%` acceptance/parity and matched baseline exact/emitted ids. Verdict: `WdB` factorization is algebraically viable and compact, and PCA-down ranks `32/64` look healthy in short 27B smokes; current Crystal-loop timings are not production speed evidence.
- [x] 2026-04-27 global/sidecar FFN PCA-down smoke: added repeated `--ffn-pca-calib-prompt=TEXT` so FFN PCA/`WdB` bases can be built from external calibration prompts instead of the eval prompt. With three external calibration prompts (`72` FFN activation vectors per layer at `calib=24`), held-out Crystal/log parsing eval kept `100%` acceptance/parity for `layers=0,2,4`, projected-DeltaNet `rank64`, and `pca-down32` on both 9B (`gen=4`) and 27B (`gen=4`). A stronger 27B `gen=8` rank sweep also kept `100%` acceptance/parity for both `pca-down16` and `pca-down32`. Caveat: DeltaNet K-bases in this probe remain prompt-local; this specifically tests whether FFN PCA/`WdB` can be sidecar/global rather than prompt-local. Verdict: sidecar adapter is plausible enough for a larger 30-50 prompt held-out falsifier.
- [x] 2026-04-27 sidecar FFN PCA-down held-out falsifier: 9B external-basis 8-prompt `gen=8` suite completed without exactness failures in the visible summary; 27B external-basis 4-prompt `gen=8` suite preserved parity on all prompts but reduced acceptance versus baseline lowrank on 3/4 prompts. Baseline lowrank stayed `100%` accepted on all 27B prompts, while `pca-down16/32` dropped to `70%`, `45.45/54.55%`, `100%`, and `87.5%` acceptance. Rechecking three affected prompts with wider `tokens=88/calib=40` and `pca-down64` only partially helped (`77.78%`, `54.55%`, `70%`). The worst JSON/db-shards prompt also failed with prompt-local dense `lowrank-ffn-pca-32/64` at the same `45.45%` acceptance as `pca-down`, proving the issue is the FFN PCA projection, not the `WdB` factorization. Verdict: exact verifier makes this safe, but current FFN PCA sidecar is not production-ready as a high-acceptance cheap draft; next gates are more calibration data, layer/rank gating, and risk-aware fallback before any Metal kernel.
- [x] 2026-04-27 all-recurrent PCA-down stress: tested `lowrank-ffn-pca-down` across all 24 recurrent layers of 9B (`0,1,2,4,5,6,...,30`) on the JSON/db-shards prompt with external FFN PCA basis. Baseline all-recurrent lowrank kept `100%` acceptance. `pca-down16` collapsed immediately (`gen=2`, `33.33%` acceptance). `pca-down32` recovered on `gen=2` (`100%`) but fell on `gen=4` (`60%`, parity still true). Verdict: all-layer PCA-down is exactly safe but acceptance-fragile; do not run expensive 27B all-layer stress until layer/rank gating is designed. Speed potential is real because dense `ffn_down` can be replaced by `rank*(n_ff+n_embd)` work per layer (tens to >100x cheaper for that one matmul), but `gate/up` remain dense and rejection economics can erase the win.
- [x] 2026-04-27 even/odd recurrent PCA-down split: on the JSON/db-shards adversary prompt with external FFN PCA basis, 9B `pca-down32` over even recurrent layers (`0,2,4,...,30`) and odd recurrent layers (`1,5,9,...,29`) both kept `100%` acceptance at `gen=4`, while the prior all-recurrent run fell at `gen=4`. At `gen=8`, even recurrent collapsed to `18.75%` acceptance, while odd recurrent held `77.78%`; increasing odd to `pca-down64` did not improve acceptance. A 27B `gen=4` smoke showed the same direction: even recurrent `60%`, odd recurrent `75%`, baseline lowrank `100%` in both. Early-even was clean in the 9B `gen=4` smoke, late-even dropped to `75%`. Verdict: odd recurrent is less toxic than even, and late-even likely contains fragile layers; parity remains safe, but the next useful sweep is layer buckets (`early/mid/late`, modulo full-attn blocks), not raw all-even/all-odd.
- [x] 2026-04-27 mod/depth bucket PCA-down sweep: on 9B JSON/db-shards `gen=8`, single `mod4` buckets showed `mod1` is least toxic (`77.78%` acceptance), while `mod0` and `mod2` were worse (`54.55%` each); combining two mod buckets was much worse (`10-18.75%`). After a too-large batch was interrupted, smaller one-bucket reruns showed all depth buckets individually pass at `gen=4`: `early_0_10`, `mid_12_22`, and `late_24_30` all kept `100%` acceptance/parity with `pca-down32`. PCA component sign/handedness is unlikely to be the direct cause because flipping a PCA vector sign leaves `(B^T a) * (W_down B)` invariant if both `B` and `WdB` are flipped consistently. The suspicious pattern is more likely phase/depth interaction: PCA errors from separate recurrent cadence buckets are tolerable alone but compound when combined. Next gate: pairwise depth bucket combinations and per-layer leave-one-out within bad combinations.
- [x] 2026-04-27 depth-pair PCA-down narrowing: pairwise depth buckets on 9B JSON/db-shards `gen=4` showed `early+mid` drops to `60%`, `mid+late` to `75%`, while `early+late` stays `100%`; however, `early+late` falls to `18.75%` at `gen=8`, so `gen=4` is too weak as a gate. Coarse leave-one-out inside `early+mid` found `early+mid_mod1` is clean (`100%`) while `early+mid_mod0` and `early+mid_mod2` both drop to `60%`. Single-depth `gen=8` reruns then showed the real stable candidate is `late_24_30`: 9B `late_24_30` stayed `100%`, while `early_0_10` fell to `25%` and `mid_12_22` to `41.67%`. A 27B `late_24_30/gen=8` smoke also kept `100%` acceptance/parity with `pca-down32`. Verdict: late recurrent bucket (`24,25,26,28,29,30`) is the first plausible stable FFN PCA-down sidecar target; early/mid need deeper gating or fallback.
- [x] 2026-04-27 27B 64-layer depth correction: the 9B-stable `late_24_30` does not mean "absolute tail" on 27B. Testing true 27B deeper buckets on JSON/db-shards showed broad late buckets are fragile: `mid_to_end_32_62` and `last_third_42_62` both fell to `20%` acceptance with `pca-down32`; narrower `last_quarter_48_62` also fell to `20%`; final tail `56,57,58,60,61,62` improved only to `40%`. Increasing the final tail to `pca-down64` did not improve acceptance (`40%`). Baseline lowrank stayed `100%` and parity stayed true throughout. Verdict: for 27B, stable PCA-down buckets are not simply "last third/end"; the promising band may be around layers `24..30` or another mid-depth island, so the next 27B sweep should scan narrow 6-layer recurrent bands (`24..30`, `32..38`, `40..46`, `48..54`, `56..62`) instead of huge suffixes.
- [x] 2026-04-27 27B narrow-band PCA-down scan: on JSON/db-shards `gen=4`, narrow 6-layer recurrent bands show a clear mid-depth island. `24..30` and `32..38` both kept `100%` acceptance/parity with `pca-down32`; `40..46` and `48..54` dropped to `75%`; `56..62` dropped to `40%`. Combining individually clean bands was not safe: `24..38` dropped to `60%`, and `24..46` also stayed at `60%`. Baseline lowrank remained `100%` everywhere. Verdict: the first 27B sidecar candidates are single narrow bands `24..30` or `32..38`, not broad suffixes and not combined islands; PCA error budget appears non-additive across bands.
- [x] 2026-04-27 PCA up/down falsifier: added `lowrank-ffn-pca-updown-R`, which trains a small ridge adapter from `ffn_in` directly to PCA activation coefficients and then applies precomputed `WdB`, bypassing dense `gate/up`, `SwiGLU`, and dense `ffn_down` in the draft FFN path. This is the first probe for cheapening the whole FFN sub-block, not only `ffn_down`. On the JSON/db-shards prompt, stable band `24,25,26,28,29,30` kept `100%` acceptance/parity on 9B (`gen=8`, `pca-updown32`) and 27B (`gen=4`, `pca-updown32`). Caveat: current timings are Crystal-loop proof timings and not Metal speed claims; the quality signal is the main result. Verdict: PCA up/down is surprisingly viable on the stable band and deserves a multi-prompt suite plus a Metal cost model.
- [x] 2026-04-27 PCA up/down held-out suite: 9B `late_24_30` (`24,25,26,28,29,30`) passed a 3-prompt external-calibration suite at `gen=8` with `100%` acceptance/parity for `lowrank`, `pca-down32`, `pca-updown16`, and `pca-updown32`. 27B single-band suites on `24..30` and `32..38` each passed parity on all 3 prompts with no exactness failures; baseline lowrank stayed `100%`, while `pca-down32`, `pca-updown16`, and `pca-updown32` all matched each other at `91.67%` average acceptance with one rejection total per band. Verdict: `pca-updown16` is not weaker than `pca-updown32` in this limited suite and can be the first Metal cost target, but 27B still has a rejection tax; next gate should run `gen=16` and a larger hostile prompt suite before production kernels.
- [x] 2026-04-27 PCA up/down `gen=16` gate: parity stayed true everywhere, but acceptance split by model/band. 9B `late_24_30` over 4 hostile prompts fell sharply: baseline lowrank stayed `100%`, while `pca-down32` averaged `74.45%`, `pca-updown16` `63.45%`, and `pca-updown32` `60.79%` acceptance. 27B was better but band-sensitive: `band_24_30` had `pca-updown32` at `100%` average acceptance (`0` rejections across 3 prompts), while `pca-down32` averaged `96.08%` and `pca-updown16` `92.98%`; `band_32_38` was weaker (`pca-updown32` `83.39%`, `pca-updown16` `89.39%`). Verdict: do not promote `pca-updown` as a production sidecar yet. Kernel work is still justified as an isolated cost/proof microkernel, with first target `27B band_24_30 pca-updown32`, because that is the only `gen=16` configuration that survived cleanly.
- [x] 2026-04-27 PCA up/down Metal microkernel gate: added probe kernels for `ffn_in -> coeffs -> WdB`, including a fused single-dispatch `ffn_pca_updown_fused` path and `Qwen35Metal.ffn_pca_updown_out_resident`. Release smokes are numerically clean: 27B layer24 rank32 hidden5120 `max_delta=2.4e-7`, `rmse=4.0e-8`; 9B layer24 rank32 hidden4096 `max_delta=2.03e-6`, `rmse=1.8e-7`. Standalone timing is not a win by itself (`27B` CPU adapter proof `0.3495ms` vs Metal `1.1456ms`, `9B` `0.2397ms` vs `1.5673ms`, with resident adapter but per-call `ffn_in` upload + wait + readback). Verdict: correctness is proven, but speed requires integration into the existing low-rank layer command buffer with no readback and a comparison against the dense FFN tail it replaces; do not ship it as a separate per-layer dispatch.
- [x] 2026-04-27 Integrated PCA up/down layer-kernel gate: added token-row kernel `ffn_pca_updown_fused_rows`, wired it into `lowrank_delta_chunk_projected_layer_async` as an optional FFN tail replacement, and added `--simulate-lowrank-metal-layer-updown=R`. This keeps `normed -> pca-updown -> residual add` inside the same low-rank layer command buffer. Combined release runs on layer24/rank64 DeltaNet + rank32 updown over a 40-token held-out chunk show a real Metal speed gate versus the dense full-layer FFN path: 27B dense full-layer `18.036ms` vs integrated updown `9.134ms` (`~1.97x`) with `max=0.00045776`; 9B dense full-layer `16.494ms` vs integrated updown `10.904ms` (`~1.51x`) with `max=0.00292206`. Verdict: the kernel shape is now promising; next step is not more standalone microbenching, but wiring sidecar resident buffers per selected layer and testing self-draft wall acceptance/speed on the 27B `24..30` band.
- [x] 2026-04-27 Whole-chain wall baseline before wiring integrated updown: 27B `layers=24,25,26,28,29,30`, external FFN calibration, `gen=8`, schedule `4,4`, `metal_project=1`, JSON/db-shards prompt preserved `100%` acceptance/parity. Current wall remains draft-bound: baseline `lowrank` reports `draft_ms=1070.831`, `verifier_ms=363.560`, `speedup_est=1.3395x`; probe-side `lowrank-ffn-pca-updown-32` reports `draft_ms=1011.050`, `verifier_ms=351.098`, `speedup_est=1.3473x`. Verdict: quality is good, but the existing wall path still uses CPU-orchestrated per-token draft and does not call the integrated layer-updown kernel; more local PCA kernel work is premature. Next useful implementation is to route selected recurrent draft layers through the integrated updown layer kernel or otherwise remove CPU orchestration/LM-head bottlenecks, then rerun this same wall gate.
- [x] 2026-04-27 Routed integrated PCA up/down into the actual wall self-draft path behind `--simulate-self-spec-wall-metal-layer-updown`. `LowRankState` now caches resident updown sidecar buffers, and `lowrank-ffn-pca-updown-R` can call the integrated Metal layer-updown route for selected recurrent draft layers. The gate is negative at the current single-token orchestration granularity: on 9B `layer24/gen4`, routed updown kept parity but was slower than lowrank baseline (`draft_ms=175.732` vs `165.920`); on 27B `layers=24,25,26,28,29,30/gen8/schedule=4,4`, routed `pca-updown32` kept parity but had `70.0%` acceptance and `draft_ms=1524.354`, essentially tied with the CPU-side updown comparison (`1519.603`) and worse than baseline lowrank (`~1150-1219`). Verdict: the integrated kernel is still useful for known-span/layer-major chunks, but wiring it as one layer dispatch/readback per autoregressive token does not accelerate self-draft. Next useful implementation must keep multiple layers/tokens GPU-resident through the draft lane and read back only logits/top1, not layer outputs.
- [x] 2026-04-27 LTP/WBA orchestration deep dive found the missing GPU-resident link for block self-draft: `forward_decode_wave_async` still starts from CPU-dequantized `emb : Array(Float32)`, so a GPU `top1_id_buf` could not feed the next draft step without CPU `embedding_lookup`. Added `embed_q4k_f32_from_token_id` plus `Qwen35Metal.embedding_q4k_from_token_id[_buf]` for Q4_K token embeddings. Accuracy is exact against CPU dequant on both local targets (`9B token 271`, hidden4096, `max=0`, `rmse=0`; `27B token 20797`, hidden5120, `max=0`, `rmse=0`). The normal self-draft Metal smoke still passes (`steps=2`, agreement `2/2`). Verdict: `top1_id_buf -> next hidden_buf` is now technically possible on GPU; next implementation should refactor decode-wave into an appendable encoder stage so one `draft_cb` can chain `gamma` token steps before any CPU readback.
- [x] 2026-04-27 First queued GPU draft chain: added `--simulate-self-draft-gpu-chain=N`, which submits dependent low-rank self-draft top1 waves back-to-back while passing each `top1_id_buf` directly as the next step's GPU token-id input. This removes intermediate CPU token readback/CPU embedding lookup, though it still uses one command buffer per token rather than one appendable block command buffer. Correctness held: 9B `layers=24/rank64/N=4` matched exact greedy `4/4` with ids `4475,71590,440,20797`; WBA showed submits were front-loaded (`submit_ms=13.150`) and execution/readback dominated (`wait_ms=71.603`, `chain_ms=84.752`, exact `86.018`). 27B `N=2` also matched exact `2/2` (`chain_ms=123.437`, exact `122.135`). Verdict: GPU `top1 -> embedding -> next top1` chaining is now proven, but not yet faster; next cut is to collapse the per-token command buffers into an appendable `draft_cb` and then overlap `draft_cb[k+1]` with verifier `cb[k]`.
- [x] 2026-04-27 First two-lane GPU draft-chain/verifier overlap gate: decode-wave submissions now accept `command_queue_name`, and `--simulate-self-draft-gpu-chain-overlap=N` submits the dependent GPU self-draft chain on a lane queue while chunk-major exact verifier runs on the default queue. This produced a real WBA overlap signal. On 9B `layers=24/rank64/N=4`, standalone draft chain was `88.695ms`, verifier was `97.339ms`, overlap wall was `129.162ms`, hidden work `56.873ms`, speedup `1.4403x`, agreement `4/4`. WBA shows verifier ran from `12.449..109.788ms`, while three draft waits were already complete and only the final wait took `19.343ms`. On 27B `N=2`, standalone draft was `137.787ms`, verifier `171.502ms`, overlap `221.877ms`, hidden `87.413ms`, speedup `1.394x`, agreement `2/2`. Verdict: unlike earlier weak one-layer overlap proxies, the GPU `top1 -> embedding` draft chain has enough resident work and queue independence to hide meaningful time behind verifier. Next cut: turn this proxy into real self-spec block scheduling with `draft_chain[k+1] || verify[k]`, then replace teacher-forced verifier candidates with previous block `draft_ids`.
  - [x] 2026-04-27 Prototype and first optimization of the real two-lane fixed-gamma block scheduler (`--simulate-self-spec-gpu-pipeline=N`): seed one GPU self-draft block, then schedule `draft_chain[k+1]` on a lane queue while the exact chunk-major verifier validates the real previous `draft_ids[k]`. Full-accept cycles keep the speculative draft state; rejects discard the next block and resync from exact state. The first version was exact but used an optimistic serial estimate; the optimized version adds a paired serial rerun, WBA markers, removes eager exact shadow replay from full-accept chunks, and reuses rollback backup buffers instead of allocating a fresh `State#fork` per verifier chunk. Final no-WBA smokes were exact: 9B `layers=0,2,4/rank64/gamma4/gen16` had `100%` acceptance/parity, `serial_ms=1212.048`, `overlap_ms=800.341`, hidden `411.707ms`, `speedup=1.5144x`; 27B `gamma2/gen8` had `100%` acceptance/parity, `serial_ms=1930.173`, `overlap_ms=1025.441`, hidden `904.732ms`, `speedup=1.8823x`. Next cut: reduce seed/last-chunk bubbles and collapse the per-token draft chain into a single appendable draft command buffer.
  - [x] 2026-04-27 Post-commit optimization probe: tested existing `QWEN35_WAVE_CHUNK_LAYERS` knob and an opt-in `--simulate-self-spec-gpu-pipeline-no-backup` policy. Larger wave command-buffer chunks (`0`, `4`, `8`) worsened actual overlap wall versus the default chunk size `2`; one giant decode-wave command buffer is not the next win. The no-backup policy remains exact by rebuilding verifier state from emitted ids on reject, but it did not improve the clean 9B wall smoke (`default overlap_ms=827.4` vs `no_backup overlap_ms=894.1` sequential), so default rollback backup stays enabled. Verdict: the next real speed work is still appendable draft-block encoding / seed-last bubble reduction, not rollback policy or wave chunk-size tuning.
  - [x] 2026-04-28 Added `--simulate-self-spec-gpu-pipeline-gammas=LIST` so real GPU scheduler gamma sweeps run in one model load. On 9B `gen=24`, all tested gammas kept `100%` acceptance/parity; actual wall (`overlap_ms`) favored `gamma=4` (`gamma2=951.151`, `gamma4=872.965`, `gamma8=862.497`, with `gamma8` only marginally lower but lower speedup ratio). On 27B `gen=16`, `gamma4` was the best wall point (`gamma2=2126.945`, `gamma4=1916.190`, `gamma8=2082.764`), while `gamma8` had the highest paired-serial ratio because its serial baseline worsened. Verdict: current scheduler sweet spot is around `gamma=4`; larger gamma is quality-safe on this prompt but draft-block seed cost dominates. This sharpens the appendable-block target: make long draft chains cheaper so `gamma=8+` improves wall, not just ratio.
  - [x] 2026-04-28 First appendable draft-block encoder slice: `forward_decode_wave_async` can now encode into a caller-owned command buffer, and the self-spec GPU pipeline probe commits one draft-block command buffer per proposed block instead of one submitted decode-wave command buffer per token. The append path disables internal wave layer chunking inside that block to preserve a single command-buffer chain from `top1_id_buf -> next embedding`. 9B gates stayed exact: short `gamma4/gen12` kept `100%` acceptance/parity and improved the same smoke from `overlap_ms=510.091` to `482.507`; `gen24` gamma sweep kept parity with `gamma4 overlap_ms=953.463` and `gamma8 overlap_ms=912.173`. WBA then exposed seed prep as `~5ms State#fork + ~38ms full-state->lowrank projection`, so `lowrank_project_state` was added and then appended into the same draft command buffer before the decode waves. The warmed WBA gate stayed exact and reduced `lr_states_self_spec_seed` to `0.040ms`, `seed_block` to `115.562ms`, and `overlap_ms` to `465.601`; the longer no-WBA `gen24` sweep improved to `gamma4 overlap_ms=910.767` and `gamma8 overlap_ms=873.237`. The initial exact `target_next_id` is now submitted after the seed draft block, so it can overlap with the seed wait; WBA showed `initial_target=35.638ms` and `wait_block_seed=75.511ms`, but `overlap_ms` now correctly includes that initial target work. The final verifier tail now skips the exact top1 after the last generated token, because no next-token target is needed once `gen_tokens` is complete; WBA reduced the final chunk from `~62-64ms` to `50.345ms`, and the fair-counted `gen24` sweep improved from `gamma4/gamma8=950.519/890.478` to `891.961/865.632`, with parity intact. Verdict: appendable state projection and final-tail skip are correct and useful; remaining work is mainly `draft_wait` after verifier. Fresh 27B append rerun is pending because no local `*27B*.gguf` was found.
  - [x] 2026-04-28 Draft-wait split experiment: added opt-in `QWEN35_DRAFT_BLOCK_TOKENS=N` and `--simulate-self-spec-gpu-pipeline-draft-splits=LIST` to split an appendable draft block into multiple committed command buffers while preserving GPU `top1_id_buf -> next embedding` chaining on the draft lane. `N=1` starts GPU draft work earliest. Short 9B `gamma4/gen12` stayed exact and cut `draft_wait_ms` (`82.052 -> 56.911`) plus `overlap_ms` (`532.066 -> 476.253`) versus unsplit. A first longer shell A/B had noisy neutral wall for gamma4 and gamma8, but in-process split sweeps reduced noise and showed `split=1` is wall-positive: `gamma4/gen24` improved `overlap_ms` `987.014 -> 882.216` in one run and `957.842 -> 902.574` in a repeat; `gamma8/gen24` improved `915.216 -> 833.438` and then `1010.432 -> 922.902`. All runs kept `100%` acceptance/parity. Verdict: split=1 is now a promoted experiment for the GPU pipeline probe, but still not a production default until WBA/repeated gates on more prompts and 27B confirm it.
  - [x] 2026-04-28 Promoted `draft_split=1` as the default GPU pipeline probe policy after a 3-prompt 9B smoke suite. Default/code/structured prompts all kept `100%` acceptance and `parity=true` for `gamma=4,8` with `gen=12`. `gamma4` overlap improved from `477.937 -> 431.674`, `459.570 -> 431.739`, and `484.867 -> 435.948`; `gamma8` improved from `537.190 -> 450.445`, `584.950 -> 448.597`, and `541.509 -> 485.576`. The structured gamma8 run showed the main caveat: `draft_wait` disappeared (`95.545 -> 0.005ms`) but verifier variance ate most of the ratio gain. The probe now defaults to `DEFAULT_SELF_SPEC_GPU_PIPELINE_DRAFT_BLOCK_TOKENS=1`, still respects `QWEN35_DRAFT_BLOCK_TOKENS`, and keeps `--simulate-self-spec-gpu-pipeline-draft-splits=LIST` for A/B sweeps. Verification: rebuilt `/tmp/qwen35_self_spec_pipeline_probe_split_default`; no-splits smoke printed `draft_split=1` with parity and `speedup=1.9513x`; explicit `--simulate-self-spec-gpu-pipeline-draft-splits=0` still printed `draft_split=0` with parity.
  - [x] 2026-04-28 Fresh local 27B gate for default `draft_split=1`: `/Users/sergey/.cache/lm-studio/models/lmstudio-community/Qwen3.6-27B-GGUF/Qwen3.6-27B-Q4_K_M.gguf` is available locally. Rebuilt `/tmp/qwen35_self_spec_pipeline_probe_split_default` and ran `--model=...Qwen3.6-27B-Q4_K_M.gguf --tokens=80 --calib-tokens=32 --ranks=64 --basis=pca --pca-iters=8 --simulate-logits-rank=64 --simulate-logits-layers=0,2,4 --simulate-generate=8 --simulate-self-spec-gpu-pipeline-gammas=4`. Result: `draft_split=1`, `accept_rate=100.0%`, `parity=true`, `overlap_ms=888.717`, `hidden_ms=784.581`, `speedup=1.8828x`. Verdict: 27B is no longer blocked; next useful 27B work is a larger prompt suite and verifier WBA, not model discovery.
  - [x] 2026-04-28 Measurement hardening: the fixed-basis probe now prints `effective_basis_rank=min..max` for the base layer and all `--simulate-logits-layers`. This exposes an important caveat in earlier `rank64/calib32` gates: PCA can only learn about `32` meaningful components from `32` calibration vectors, while the Metal low-rank path still allocates/loops over the requested `rank=64` slots. Verification: rebuilt `/tmp/qwen35_self_spec_pipeline_probe_basis_note`; `--tokens=40 --calib-tokens=32 --ranks=64 --basis=pca --pca-iters=2` prints `effective_basis_rank=32..32 requested_rank=64 note=requested_rank_exceeds_some_effective_bases`. WBA on the current 9B `gamma4/gen12/split1` gate shows the remaining bubble is not seed projection (`lr_states=0.030ms`) but non-final chunk draft waits after verifier (`~26-28ms`), so the next speed lever is cheaper draft blocks or verifier reshaping, not rollback.
  - [x] 2026-04-28 Added plain-exact timing to the GPU self-spec probe. `speedup` remains paired serial draft+verify vs overlapped draft+verify, while new `plain_exact_ms`/`plain_speedup` compares against ordinary exact greedy target decode from the same prompt state. First honest `calib64/rank64` gate is a falsifier for the current low-rank GPU self-spec as a plain-decode win: 9B `gen12/layers=0,2,4/split1` kept `100%` acceptance/parity, but `gamma4` measured `plain_exact_ms=326.255`, `overlap_ms=541.670`, `plain_speedup=0.6023x`; `gamma8` measured `plain_exact_ms=327.236`, `overlap_ms=574.608`, `plain_speedup=0.5695x`. llama.cpp support check: local `llama-speculative-simple` runs Qwen3.5 9B target + Qwen3.5 0.8B draft successfully and accepted `8/8` draft tokens on a short smoke, but the available llama.cpp speculative path is external-draft/tree speculation, not our same-weight low-rank self-draft.
  - [x] 2026-04-28 Added GPU-pipeline cheap-draft research switches and an exact tail-draft scheduler fix. `--simulate-self-spec-gpu-pipeline-draft-no-ffn` now mirrors the CPU `lowrank-no-ffn` idea inside GPU self-spec by skipping FFN only on selected low-rank recurrent draft layers; the exact verifier remains unchanged. On 9B `gen12/gamma4/calib64/rank64`, `layers=0,2,4` improved `plain_speedup` from `0.5721x` to `0.5968x` with `100%` acceptance/parity, and expanding to `layers=0,2,4,6,8,10` kept `100%` acceptance/parity with `plain_speedup` in the `~0.64-0.75x` range depending on timing noise. Wider `12` selected layers was refuted despite clean static drift: real self-spec acceptance fell to `38.1%` and `plain_speedup=0.2502x`, proving that speculative acceptance is the controlling gate. A stronger `--simulate-self-spec-gpu-pipeline-draft-skip-recurrent-ffn` switch was also tested and refuted immediately (`0%` acceptance, `plain_speedup=0.1464x`), so naive all-recurrent FFN skipping is not a useful draft route. Separately, draft blocks now encode only the remaining required proposal tokens instead of always emitting `gamma`; on `gamma8/gen12/layers=0,2,4,6,8,10/draft_no_ffn`, this cut tail `draft_next_ms` from `63.150ms` to `17.353ms` and improved `plain_speedup` from `0.5619x` to `0.6543x` with parity intact. Verdict: exact scheduler trimming is worth keeping; no-FFN selected-layer draft is useful as a research knob but still not a plain-decode win.
  - [x] 2026-04-28 Ported progressive gamma schedules into the real GPU self-spec pipeline via `--simulate-self-spec-gpu-pipeline-schedule=LIST`. The schedule repeats on full accepts and resets to the first entry on reject, while exact verifier parity remains mandatory. First real 9B gate on `layers=0,2,4,6,8,10/rank64/draft_no_ffn/gen32` refuted naive `4,4,8`: it kept parity but reset repeatedly (`gamma_history=4,4,4,4,4,4,4,4,4,2,1`), accepted only `71.79%`, and measured `plain_speedup=0.3989x`. Fixed `gamma4` on the same setup was also not a win (`82.86%` accept, `plain_speedup=0.4884x`). A conservative `layers=0,2,4` long gate restored `100%` acceptance for `gamma4` and `gamma8`, but still measured only `~0.64-0.66x` plain speedup. Verdict: real progressive scheduling is useful instrumentation, but the current same-weight draft remains too expensive; the next major lever must reduce draft cost further or use an off-GPU/external draft.
  - [x] Calibration probe `--simulate-lowrank-multilayer-chunk-thread-overlap=N` swept N=1..48 chained low-rank layer chunks on lane queue parallel to chunk-major verifier on default queue (9B/rank64/layer=2/96-tok prompt/calib=64). Speedup: N=1→0.948x, N=4→0.963, N=8→0.981, N=16→1.000, N=24→1.032, N=32→1.031, N=48→1.051. Verifier wall is rock-stable at 165ms regardless of N — chunk-major saturates GPU, lane work largely serializes. Re-running pre-existing 2-lane independent-chunk test (`--simulate-lowrank-metal-layer-overlap`) at default flags gives 1.07-1.20x, NOT the 3.26x reported on line 305-306. Verdict: cross-queue parallelism on M2 Max for this workload caps at ~1.05-1.20x wall savings, far short of the historical 3x. The "draft on lane || chunk-major verifier on default" lever alone is unlikely to close Phase 4.5 (need ~5pp more decode speedup). Pivot candidates: (a) shrink chunk-major verifier so it leaves GPU idle for draft, (b) move draft compute off GPU (CPU has 8 perf cores), (c) accept ~1.05-1.10x as the realistic ceiling for this architecture.
  - [ ] Research branch: explore "dynamic depth" as a two-mode controller, not direct unchecked approximation. Speed mode dynamically adjusts `gamma/rank/layer-set/verifier` under exact verification. Reasoning mode spends saved budget on top-k/tree futures or self-check/rerank from the low-rank recurrent branch, then exact-verifies/chooses; do not mutate final answers from repeated recurrent blocks unless a verifier or training objective validates it.
  - [ ] Next approximate branch: extend the self-spec low-rank harness to a paired prompt suite (`30-50` prompts, `gen=32-64`) and test progressive schedules (`4,4,8`, `4,8,8`, `4,4,8,16`) against fixed `gamma=4/8/16`, plus beam/progressive tree verification that branches before generating/verifying a long wrong tail. Report both sum and overlap cost models, then prototype a two-lane draft/verifier implementation only if the overlap model remains strong after adding realistic resync/command-buffer overhead. Prioritize rank `32/48/64` and layer sets (`0,2,4`, `0,2,4,8`, early6).
  - [ ] Continue DeltaNet scan research only with a different rank-stable formulation that avoids GPU-serial elimination and avoids dense `128x128` products, or pause this branch and retest long-prefill WBA/kernel ideas. Target `pp1024+`; do not wire the current multi-dispatch compact route into production prefill.

## Phase 5 — Scale to 27B

- [ ] **5.1** Run all tests on 27B Q4_K_M
- [ ] **5.2** mlock balance — avoid SIGBUS (page eviction) vs SIGKILL (wired memory pressure)
- [ ] **5.3** Final tok/s on 27B

## Phase VL — optional

- [ ] **V.1** CLIP vision encoder (27 blocks, 1152d, patch 16, 768×768 images)
- [ ] **V.2** qwen3vl_merger projector (1152 → 4096, spatial_merge=2)
- [ ] **V.3** Deepstack multi-layer injection
- [ ] **V.4** Image preprocessing pipeline
- [ ] **V.5** `VLAdapter` → `CausalDecoder.forward(embeddings, mrope_positions)` integration

---

## Correctness criterion (all phases)

Per-token cosine similarity vs llama.cpp HEAD on standard test prompt:
- Prefill 64 tokens from `"The quick brown fox jumps over the lazy dog. Describe this scene in detail:"`-style prompt
- Decode 64 tokens
- Target: cosine ≥ 0.9999 per logit vector

## Speed criterion

Wall-clock tok/s measured with `/usr/bin/time`:
- Prefill tok/s (full 64 tokens)
- Decode tok/s (averaged over 64 tokens)
- KV save/load round-trip time
- Baseline: llama.cpp HEAD with identical settings (no-flash-attention default, as in previous bench)

## Status

- **Active phase:** 4 (optimization: beat llama.cpp)
- **Active task:** 4.9 (true layerwise/microbatch prefill)
- **Baseline (llama.cpp 86db42e97):** 9B Q4_K_M pp64=458 / tg64=43.5 tok/s (FA=0) → targets ≥504 / ≥48
- **Latest guarded benchmark:** prompt64/gen64 reps=5/warmup=2 with `--require-quiet --wait-quiet-ms=60000`: native prefill `426.01 tok/s` p50 vs llama.cpp `461.90 tok/s` (`-7.77%`), native decode `47.60 tok/s` p50 vs llama.cpp `45.35 tok/s` (`+4.96%`)
- **Latest relaxed-load benchmark after shared-H16 recurrent projection cleanup:** prompt64/gen64 reps=3/warmup=1: first-run native prefill `426.79 tok/s` p50 vs llama.cpp `459.43 tok/s` (`-7.10%`), preallocated-state prefill `448.60 tok/s` vs llama.cpp `465.91 tok/s` (`-3.71%`), prompt-cache restore `1350.65 tok/s` vs llama.cpp `465.80 tok/s` (`+189.97%`), and native decode remains ahead by `+4.05..4.47%`.
- **Latest self-spec kernel finding:** real GPU pipeline now has resident FFN `pca-updown` draft routing via `--simulate-self-spec-gpu-pipeline-draft-updown=R` and in-process A/B via `--simulate-self-spec-gpu-pipeline-draft-updowns=LIST` (`0` = lowrank baseline). On Qwen3.6-27B, layers `24,25,26,28,29,30`, rank64, `gen=16`, schedule `4,4,8`, `pca-updown16` preserved `parity=true`/`100%` accept and improved `overlap_ms` from `2353.995` to `1818.438` (`~22.8%` relative) versus baseline lowrank, but still measured only `plain_speedup=0.5616x`; this is a useful draft-cost/acceptance improvement, not yet a direct plain-decode win.
- **Latest external-calibration falsifier:** with three external FFN calibration prompts on the same 27B band and `gen=16/schedule=4,4,8`, `pca-updown16` kept parity on 3/3 eval prompts and improved overlap on default/code, but did not robustly improve acceptance: default `83.33% -> 100%`, code `78.95% -> 78.95%`, JSON/db-shards `78.95% -> 68.42%`. Rechecking JSON with `pca-updown32` kept the same `68.42%` acceptance. Treat prompt-local pca-updown as promising, but external/global sidecar needs prompt-risk fallback or better calibration before production.
- **Latest risk-controller finding:** added `--simulate-self-spec-gpu-pipeline-draft-updown-fallback-on-reject`, which starts with `pca-updown` but disables it for future draft blocks after the first verifier rejection. On the external-calibration JSON/db-shards falsifier, fallback-on-reject preserved parity and improved `pca-updown16` from `68.42%` to `93.75%` acceptance, with `overlap_ms=1855.882` versus lowrank baseline `2197.082`; this is the first practical guard that makes the pca-updown speed lever less brittle.
- **Latest suite-harness finding:** added `--simulate-self-spec-gpu-pipeline-suite-prompt=NAME::TEXT` so one process can reuse loaded weights and external FFN adapters while rebuilding prompt-local K bases for additional eval prompts. The harness fail-closes when pca-updown suite runs without external `--ffn-pca-calib-prompt`, avoiding accidental reuse of main-prompt FFN adapters as if they were global. A 9B smoke with main+suite prompts, baseline vs `pca-updown16`, fallback-on-reject, and external calibration preserved parity across all suite rows.
- **Latest 27B suite falsifier:** in one-process Qwen3.6-27B external-calibrated suite (`main`, `code_fibonacci`, `json_db_shards`; `gen=16`, schedule `4,4,8`, layers `24,25,26,28,29,30`), `pca-updown16+fallback` preserved parity in all rows but was not a universal win: main improved overlap `2316.299 -> 2160.379` at same `83.33%` acceptance, code regressed `2055.370 -> 2619.448` and `83.33% -> 66.67%`, JSON improved acceptance `78.95% -> 93.75%` but worsened overlap `2916.710 -> 3336.802`. Next controller must be predictive/prompt-aware or schedule-aware; first-reject fallback alone is not enough.
- **Latest schedule-aware controller:** added `--simulate-self-spec-gpu-pipeline-draft-updown-after-full-accepts=N` and `draft_updown_chunks` reporting. The pca-updown draft route now can start disabled, enable only after N consecutive full-accept chunks, and reset/disable on any reject. Because the overlap scheduler pre-submits the next block before verification, enabling can lag by one block; a 9B `gen=6/schedule=2,2,2` smoke confirmed the lagged enable path with `draft_updown_chunks=1`, `accept_rate=100%`, and `parity=true`.
- **Latest warmup falsifier:** the same 27B 3-prompt suite with `--simulate-self-spec-gpu-pipeline-draft-updown-after-full-accepts=1` preserved parity but did not beat baseline. Warmup used exactly two pca-updown chunks per prompt and regressed main acceptance/overlap (`83.33%/2293.430 -> 73.68%/2328.482`), code acceptance/overlap (`83.33%/2051.861 -> 78.95%/2128.309`), and JSON overlap (`2893.256 -> 2944.803` at same `78.95%`). Streak-only warmup is not enough; next pca-updown controller needs a predictive risk signal before enabling or should be deprioritized behind other exact levers.
- **Blocked:** nothing
- **Last updated:** 2026-04-29
